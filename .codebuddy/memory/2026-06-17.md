# 2026-06-17

## 侧边栏悬浮/停靠重构
- 移除 `[data-tauri-window] .sidebar-floating { left: -26px; }` CSS 规则
- 删除 `sidebarFloating` 配置、`.sidebar-docked` 样式、`App.tsx` 中 `pl-[56px]` 条件逻辑
- 侧边栏恢复为纯 `.sidebar-floating` 模式

## 自定义背景修复
- `SettingsPanel.tsx`：点击「自定义图片」时，若已有 `customBackgroundUrl`，直接切换主题不再弹出文件选择器
- 仅无已保存背景时才触发文件选择

## 自定义背景自动切换主题
- `handleBgFileChange`：上传背景识别为浅色 → `theme: 'light'`；深色 → 保持当前主题不变
- 切换到已有自定义背景时：根据 `customBackgroundIsDark` 自动切主题（浅色切 light，深色保持当前主题）
- `BACKGROUND_OPTIONS` 新增 `theme: 'dark' | 'light'` 字段标记每个内置背景的明暗
- 切换内置背景时同步自动切换主题（default/solar-system/nebula → dark, off-white → light）
