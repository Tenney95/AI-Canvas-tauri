# 2026-06-03

- **feat: 选中连线后按 Backspace/Delete 可删除**: 在 `useKeyboardShortcuts.ts` 的 Delete/Backspace 处理中增加对选中连线的删除支持。React Flow 选中连线时 `edge.selected === true`，通过筛选 `selectedEdgeIds` 删除。先删连线再删节点，避免节点删除逻辑清理连线时冲突。修改文件: `useKeyboardShortcuts.ts`
- **fix: 连线无法选中的根因修复**: `store.onEdgesChange` 是空操作（仅 console.log），React Flow 发出的边选中变更（`{ type: 'select', selected: true }`）从未应用到 edges 上。修复：`Canvas.tsx` 中导入 `applyEdgeChanges`，新增 `handleEdgesChange` 回调，用 `applyEdgeChanges` 将变更应用到 edges；ReactFlow 添加 `onEdgesChange={handleEdgesChange}` 属性。修改文件: `Canvas.tsx`
- **fix: edges is not iterable 运行时崩溃**: `handleEdgesChange` 中调用 `setEdges((eds) => applyEdgeChanges(...))` 传的是函数给 Zustand 的 `setEdges` action，但该 action 实现是 `set({ edges })` 会把函数对象当成 edges 的值，导致 edges 变成非数组。修复：改用 `useAppStore.setState((s) => ({ edges: applyEdgeChanges(changes, s.edges) }))` 直接做函数式更新。修改文件: `Canvas.tsx`\n- **feat: 连线选中高亮**: 在 `reactflow.css` 中添加 `.react-flow__edge.selected .react-flow__edge-path` 样式，选中边路径变为 indigo (`#6366f1`)、线宽增至 2.5；箭头 marker 同步变色。同时给默认边路径加 `transition` 平滑过渡。修改文件: `reactflow.css`
- **refactor: ImageNode 组件瘦身**: 将 737 行拆分为 ~255 行 + 3 个 shared 组件。提取：`ResizeHandle.tsx`（通用缩放把手，无 store 依赖）、`MattingEditor.tsx`（遮罩编辑器全屏覆盖层，通过 props 回调通信）、`imageUtils.ts`（computeImageNodeDimensions 工具函数）。ResizeHandle 和 MattingEditor 均可被其他节点复用。修改文件: `ImageNode.tsx`, 新建: `shared/ResizeHandle.tsx`, `shared/MattingEditor.tsx`, `shared/imageUtils.ts`\n- **commit**: `8ef24fa` refactor: ImageNode 组件瘦身 (4 files, +523/-467)
- **feat: 安装 vercel-labs/agent-skills**: 通过 `npx skills add vercel-labs/agent-skills -y` 本地安装了全部 10 个 Vercel 官方技能到 `.agents/skills/`，已 symlinked 到 CodeBuddy。核心技能：react-best-practices（40+ 规则）、web-design-guidelines（100+ 规则）、composition-patterns（组合模式）、writing-guidelines（80+ 规则）。安装路径: `d:\www\project\AI-Canvas-tauri\.agents\skills\`
- **fix: 模板字符串断句导致 3 个文件 PARSE_ERROR**: `store.nodes.ts`、`store.clipboard.ts`、`store.groups.ts` 中的模板字符串 `` `prefix-${generateId()}` `` 被截断为 `prefix-;`，导致 oxc 解析失败。修复：补全7处模板字符串 + 补齐 `generateId` import。同时修复 `store.groups.ts` 中 `name: 分组` 缺少引号的问题。
- **fix: APIMart 轮询返回扁平 JSON 导致 `task.status` 报 undefined**: APIMart 提交接口返回 `{ code, data: [{ task_id, status }] }`（有 data 包裹），但轮询接口 `/v1/tasks/{id}` 返回扁平格式 `{ status, message, task_id }`（无 data 包裹）。`aiService.ts` 中 `generateApimartImage` 和 `generateApimartVideo` 的 `const task = pollResult.data;` 在轮询时取到 `undefined`。修复：改为 `const task = pollResult.data ?? pollResult;`（与 `apimartService.ts` 一致），同时将类型定义中的 `data` 字段改为可选并增加扁平字段的 union 类型。修改文件: `src/services/aiService.ts`\n- **feat: GSAP 开屏动画强化版 (SplashScreen v2)**: 重写 `src/components/SplashScreen.tsx`。新增：4 种 AI 节点类型卡片（Text/T、Image/I、Video/V、Audio/A）在菱形四角排列，配色与项目 NodeLabel 一致（indigo/green/blue/orange）。动画流程：画布网格+多层径向渐变背景 → 18个彩色粒子随机漂浮 → 4张卡片从四角弹性飞入（back.out）并亮起光晕 → SVG 贝塞尔连线从卡片向中心绘制（strokeDashoffset）→ 中央 Logo 弹性缩放+三层光晕 → 标题模糊淡入 → 呼吸脉冲（Logo光晕+连线闪烁）→ 退出：连线收回+卡片飞出+Logo放大模糊+背景淡出。使用 gsap.context 管理生命周期，CardConfig 配置化。修改文件: `src/components/SplashScreen.tsx`\n- **fix: SplashScreen v3 — 卡片重叠 + X形丑线修复**: (1) 卡片飞入目标从 `x:0, y:0`（全部汇聚中心重叠）改为 `x: tx, y: ty`（各自不规则分散位置，`text: -165/-125, image: 178/-115, video: -160/132, audio: 185/128`）；(2) 去掉 4 条汇聚到中心的贝塞尔线，替换为卡片间装饰环形弧线（左上→右上→右下→左下→左上 成环）+ 两层中心虚线装饰环（r=56/r=36）+ 中心亮点，弧线逐段 strokeDashoffset 动画绘制；(3) 卡片使用 `left:50%;top:50%` + `transform:translate(-50%,-50%)` 居中，GSAP `x/y` 在 CSS transforms 之后叠加偏移。修改文件: `src/components/SplashScreen.tsx`\n- **feat: SplashScreen v4 — 胶片放映机+Logo SVG变形**: (1) 卡片过场改为胶片放映机风格：添加上下齿孔（SprocketHoles 组件，7孔排列，颜色匹配节点色）、边框变为上下着色（`borderTop/Bottom` 使用节点色 20% 透明）+ 左右半透明；自动画新增放映机启动闪烁（brightness 1.4→0.8→1.0 steps）、卡片逐帧步进滑入（从上下 overshoot 再 back.out 回弹）、退出时再次闪烁+滑出。(2) Logo 从 `<img>` 改为内联 SVG（`LogoInlineSvg` 组件），直接操作 sparkle 四角星路径（`attr: { d }`）：入场 wide→orig（elastic.out），呼吸漫游 orig↔tall（sine.inOut），关屏 wide 压扁；核心圆 r 脉冲（45→50→45）；三个代表节点的白点依次脉冲（r 12→14→12）。所有 SVG 动画通过 GSAP `attr: {}` 驱动。修改文件: `src/components/SplashScreen.tsx`\n- **refactor: SplashScreen v5 — 极简黑白光影**: 砍掉所有复杂元素（4张卡片、齿孔、粒子、装饰弧线、彩色渐变），仅保留 Logo 描边线条 + "AI Canvas" 应用名。Logo 从填充彩色版改为纯描边版（Squircle 轮廓 + Sparkle 四角星 + 中心圆，全部 `stroke="white"` 无填充）。动画流程简化为：极暗中心漏光（radial-gradient vignette）→ Squircle 轮廓渐现 → Sparkle 描边逐线浮现 → 中心圆描边渐显 → Logo 外围微弱环光（boxShadow）→ 标题淡入 → 光晕呼吸脉冲 + 标题明暗微变 → 短暂停顿 → 整体淡出。纯黑白，无色彩，代码量从 ~555 行缩减至 ~190 行。修改文件: `src/components/SplashScreen.tsx`\n- **fix: Logo 直角黑边 + 标题光影滑过**: (1) Squircle 路径添加 `fill="rgba(255,255,255,0.015)"` 填充，消除 SVG viewBox 透明角落与暗角光背景对比产生的生硬直角黑边；(2) 标题新增光条扫过效果：`lightSweepRef` 半透明渐变条从左滑到右掠过文字背后（`left: '-100%' → '100%'`），同步驱动 `titleInnerRef` 文字 `textShadow` 偏移模拟光源移动（左→中→右→回落正下方）；呼吸阶段 textShadow 在上下方向微幅摆动（`y: -1px ↔ 1px`），增加立体光影层次感。修改文件: `src/components/SplashScreen.tsx`\n- **refactor: SplashScreen v6 — Hatom 风格宇宙光影**: 参考 hatom.com 的黑暗虚空+光点爆发设计语言，重构为 7 阶段叙事动画：(1) Void — 24个宇宙微尘（CosmicParticles）逐个浮现；(2) Ignition — 中心光点（radial-gradient）亮起+脉动；(3) Rays — 8条辐射光线（LightRays）scale+opacity 渐现，缓慢旋转模拟星云扩散；(4) Emergence — Logo 弹性浮现（back.out）+ 18个炸裂粒子（BurstParticles）从中心向四周飞散（x/y 偏移+淡出）；(5) Revelation — 标题浮现+光条扫过；(6) Breathe — Logo环光+标题投影+中心光三重同步脉冲；(7) Exit — 光线+微尘消散+整体淡出。新增3个子组件：CosmicParticles（24个随机位置微尘）、LightRays（8条渐变光线）、BurstParticles（18个data-tx/data-ty驱动飞散粒子）。修改文件: `src/components/SplashScreen.tsx`\n- **fix: 移除炸裂粒子 + 全线缩短动画时长**: 删除 BurstParticles 组件（18个飞散小点）及其 GSAP 动画。所有 duration 缩减约 40-50%：微尘 1.2→0.5s、中心光 0.8→0.4s（脉动 0.4+0.5→0.2+0.25s）、光线 0.6→0.3s（旋转 4→2s）、Logo 入场 0.7→0.4s、标题浮现+光条扫过 1.0→0.5s、呼吸 1.2→0.7s、退出 0.4+0.6→0.2+0.3s。整体动画时长从 ~9s 压缩至 ~5s。修改文件: `src/components/SplashScreen.tsx`\n- **fix: 移除光线辐射 + 再度缩短时长**: 删除 LightRays 组件（8条渐变辐射光线 + 旋转动画）及其所有 GSAP 引用（querySelector/set/to）。动画从 7 阶段缩减为 6 阶段（Void→Ignition→Emergence→Title→Breathe→Exit）。所有 duration 再缩减约 50%：微尘 0.5→0.25s、中心光 0.4→0.2s（脉动 0.2+0.25→0.1+0.12s）、Logo 入场 0.4→0.25s、描边 0.25+0.3+0.2→0.15+0.18+0.12s、环光 0.3→0.18s、标题浮现 0.4→0.25s、光条扫过 0.5→0.3s、呼吸 0.7→0.4s、退出 0.2+0.3→0.1+0.2s。总体约 ~2.5s。修改文件: `src/components/SplashScreen.tsx`\n- **commit**: `25a5c09` feat: GSAP 开屏动画 + Apimart 轮询兼容修复 (5 files, +442/-16)
- **commit**: `a5bfd3d` feat: 弹窗/菜单屏幕边界检测 (10 files, +484/-19)
- **fix: SlashCommandMenu 缺少边界检测**: 主菜单位置从硬编码 `left: rect.left + 40` / `top: rect.top - 50*n - 100` 改为通过 `useLayoutEffect` 估算尺寸后调用 `calcFixedPosition` 自动贴边；子菜单从硬编码 `left: menuPos.left + 268` 改为 `calcSubmenuPosition` 检测右侧空间不足时翻转到左侧。新增 `menuPos` 和 `submenuPos` state。修改文件: `SlashCommandMenu.tsx`
- **feat: 弹出框/选择框/下拉菜单/二级菜单溢出优化**: 新增 `src/utils/popupPosition.ts` 工具库（calcFixedPosition/calcSubmenuPosition/calcDropdownPosition/calcTooltipPosition）。修改 6 个组件全部添加屏幕边界检测：
  - `CanvasContextMenu.tsx` — L1 菜单用 calcFixedPosition 限位，L2/L3 子菜单通过 useLayoutEffect 测量 DOM 后用 calcSubmenuPosition 动态计算方向（右→左翻转）+ 位置，替代硬编码偏移（+180/+364）
  - `NodeContextMenu.tsx` — calcFixedPosition 限位
  - `ConnectionMenu.tsx` — calcFixedPosition 限位，用 useMemo 避免重复计算
  - `NodeMenu.tsx` — calcFixedPosition 限位
  - `ModelSelector.tsx` — useLayoutEffect 检测上下空间，动态设置 `.drop-down`（向下弹出）和 `.drop-align-right`（右对齐）CSS 类，替代固定 `bottom:100%`
  - `Sidebar.tsx` — DeleteConfirm dialog 用 JS 计算 viewport 安全位置（上方不足时翻到下方）+ 箭头方向 `.delete-confirm-above`
  - `model-selector.css` — 新增 `.drop-down`/`.drop-align-right` 类
  - `sidebar.css` — 新增 `.delete-confirm-above` 箭头向上样式 + `.node-picker`/`.avatar-menu` max-width 限制
  - `settings.css` — tooltip/error-tip 增加 `max-width: min(..., calc(100vw - 32px))` 防止水平溢出
  - TypeScript 编译通过（仅预存错误，无新增）
