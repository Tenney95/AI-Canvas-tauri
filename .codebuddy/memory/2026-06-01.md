# 2026-06-01

- **feat: 对话框中输入 `/` 弹出快捷命令选择栏**：`MentionEditor` 新增 `onSlashTrigger` prop，`handleInput` 中检测 `/` 字符（与 `@` 检测并列），触发回调。`PromptPanel` 新增 `promptInputRef` 和 `slashTriggerSource` ref，编辑器触发的 slash 菜单以输入框容器为锚点定位，按钮触发的仍以原 slash 按钮为锚点。
- **feat: 用户自定义预设系统（PresetManager）**：新增完整的用户预设 CRUD 体系：
  - **类型** (`types/index.ts`)：新增 `UserPreset`、`PresetNodeType`、`PresetTriggerMode` 及辅助常量
  - **IndexedDB** (`indexedDbService.ts`)：DB 升至 v4，新增 `presets` ObjectStore 和 CRUD
  - **服务层** (`fileService.ts`)：新增 `savePreset`/`loadPresets`/`deletePreset` 封装
  - **Store** (`useAppStore.ts`)：新增 `userPresets`、`presetManagerOpen` 状态及 CRUD actions + `initFromDb` 加载
  - **PresetManager** (新组件)：弹窗管理用户预设，支持按节点类型分 tab（文本/图像/视频/音频）、新建/删除/编辑预设、缩略图上传、contentEditable 提示词模板编辑器（含 `{{ 文章内容 }}` 占位 pill）、触发模式切换（直接触发 / 加入提示词）
  - **SlashCommandMenu**：新增 `userPresets` 和 `onManagePresets` props，菜单中展示用户预设列表（含缩略图），底部「管理预设」入口替换原「即将推出」占位
  - **样式** (`preset-manager.css`)：弹窗、tab、侧边栏列表、详情编辑区、footer 操作栏等完整样式
- **提交 `b7a053f`**：10 files, +1293/-51, feat: 用户自定义预设系统 + / 快捷命令菜单
- **优化 PresetManager 蒙版**：`.preset-modal-overlay` 蒙版背景加深（0.6→0.65），新增 `backdrop-filter: blur(6px)` 毛玻璃效果，增加淡入动画（`presetOverlayFadeIn`）。`.preset-modal--manager` 弹窗新增弹性缩放入场动画（`presetModalSlideIn`），scale 0.95→1 + translateY。
- **feat: 多选+分组系统**：
  - **类型** (`types/index.ts`)：新增 `NodeGroup` 接口（id/name/nodeIds/color）和 `GROUP_COLOR_PALETTE` 色板
  - **Store** (`useAppStore.ts`)：新增 `groups` 状态、`groupSelectedNodes()` 和 `ungroupSelectedNodes()` actions，分组自动命名/分配颜色，解散时清理
  - **Hook** (`hooks/useGroupSelection.ts`)：独立 hooks 文件，监听 Ctrl+G / Alt+G 全局快捷键，智能判断分组/取消分组（选中节点全在同一分组→解散，否则→创建），编辑状态下跳过
  - **Canvas.tsx**：`onNodeClick` 增加 `e.shiftKey` 判断（Shift+点击时不弹对话框以支持多选）；ReactFlow 增加 `selectionOnDrag`（鼠标框选）和 `multiSelectionKeyCode="Shift"` 属性；新增 `GroupOverlay` 组件，计算分组节点包围盒，渲染带颜色标签的半透明背景矩形（viewport 坐标变换）
  - **持久化**：`ProjectSaveData` 新增 `groups` 字段，`saveCurrentProject` / `switchProject` / `initFromDb` / `createProject` 均支持 groups 的存取
- **style: 分组框和选择框加 class 名**：`reactflow.css` 新增 `.canvas-group-overlay`/`.canvas-group-box`/`.canvas-group-label` 三个 class，将 GroupOverlay 静态内联样式迁移至 CSS；`.react-flow__selection` 新增 `border-radius: 8px`
- **feat: 分组标题编辑 + 整组拖拽**：
  - **Store**：新增 `renameGroup(id, name)` action，trim 后更新分组名
  - **Canvas.tsx GroupOverlay**：标题改为 `contentEditable` span（Enter 确认/Escape 取消/blur 提交），新增拖拽手柄 `canvas-group-handle`（分组顶部颜色条）；`mousedown` 记录起始鼠标+节点位置，document 级 `mousemove` 计算 delta/zoom 后通过 `setNodes` 实时更新全组位置，`mouseup` 提交历史
  - **CSS**：新增 `.canvas-group-handle`（grab 光标/hover 高亮）、`.canvas-group-label-text`（contentEditable 边线/focus 光晕）、`.canvas-group-count`（节点数）等样式
  - GroupOverlay 改为直接从 store 读数据（不再接收 props），修复 `overflow: hidden` → `visible` 裁切问题
- **fix: 分组 overlay z-index 过低**：`.canvas-group-overlay` 的 `z-index: 0` 被 React Flow 内部层遮挡，改为 `z-index: 10` 使分组框可交互
- **refactor: 分组从 overlay 层迁移为 React Flow 原生 parentId 节点（解决拖拽不同步）**：
  - **新增** `src/components/nodes/GroupNode.tsx`：作为 `type: 'group'` 的自定义节点，contentEditable 标题、handle bar、子节点计数，通过 `parentId` 查询子节点
  - **删除** `src/components/canvas/GroupOverlay.tsx`：不再需要独立的 viewport transform 叠加层
  - **Store** (`useAppStore.ts`)：`groupSelectedNodes()` 改为创建真正的 React Flow group 节点（计算包围盒，设置 `parentId` 和相对坐标）；`ungroupSelectedNodes()` 改为移除 group 节点并还原子节点绝对坐标；`renameGroup()` 同步更新 group 节点的 `data.label`
  - **Canvas.tsx**：注册 `GroupNode` 到 `nodeTypes`；`handleNodesChange` 拦截 group 节点删除改为 ungroup（子节点还原绝对坐标+清除 parentId）
  - **CSS** (`reactflow.css`)：移除 `.canvas-group-overlay`/`.canvas-group-box`，新增 `.canvas-group-node-wrapper`/`.canvas-group-node-box`，handle 改为 `position: absolute; top: 0` 内嵌于节点
- **fix: 分组快捷键 Ctrl+G 无效**：将分组快捷键逻辑从独立 `useGroupSelection` hook 合并到 `useKeyboardShortcuts` 统一管理（避免两个 capture-phase listener 的执行顺序问题）；`App.tsx` 移除 `useGroupSelection()` 调用；`groupSelectedNodes()` 静默返回改为 toast 提示（`selectedNodeIds < 2` 或 `candidateIds < 2`）
- **fix: GroupNode 无限重渲染**：`useAppStore((s) => s.nodes.filter(...))` 中 `filter()` 每次返回新数组引用，导致 `useSyncExternalStore` 的 `getSnapshot` 触发无限循环。改为 `s.nodes.filter(...).length` 取数字（原始类型，值相等即稳定）
- **refactor: 分组样式迁移+优化**：将分组相关 class（`.canvas-group-node-*`）从 `reactflow.css` 移至 `nodes.css`。样式优化：虚线边框 + `backdrop-filter: blur(12px)` 毛玻璃、handle bar 加高至 32px + `::before` 色点指示器 + 聚焦态发光 glow、计数 badge `margin-left: auto` 靠右。`GroupNode.tsx` handle bar 增加 `color` inline style 传递分组色给伪元素。
- **提交 `a18e483`**：9 files, +547/-7, feat: 节点分组 GroupNode — 多选+分组/解组
- **feat: 分组可调整大小 + 拖出/拖入自动离组/入组**：
  - **GroupNode.tsx**：引入 `NodeResizer`（minWidth=200, minHeight=120），右下角新增 `.canvas-group-resize-hint` 视觉提示，hover 时显示
  - **Canvas.tsx**：新增 `handleNodeDragStop` 包装函数（替代直接的 `onNodeDragStop`）：
    - 拖出分组范围（节点 center 不在父组 bounds 内）→ 清除 `parentId`、转绝对坐标、从 `groups.nodeIds` 移除
    - 拖入分组范围（自由节点 center 在某组 bounds 内）→ 设置 `parentId`、转相对坐标、加入 `groups.nodeIds`
    - 自动删除变空的分组节点
    - 先执行分组逻辑、再调用 snap 的 `onNodeDragStop`（仅一次 commitToHistory 捕获最终状态）
  - **CSS**：`.canvas-group-resize-hint` 渐变角标 + hover 显示；覆盖 NodeResizer handle/line 样式
- **提交 `56920a9`**：3 files, +215/-42, feat: 分组可调整大小 + 拖出/拖入自动离组/入组
