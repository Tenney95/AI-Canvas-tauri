# 2026-06-01

- **feat: 对话框中输入 `/` 弹出快捷命令选择栏**：`MentionEditor` 新增 `onSlashTrigger` prop，`handleInput` 中检测 `/` 字符（与 `@` 检测并列），触发回调。`PromptPanel` 新增 `promptInputRef` 和 `slashTriggerSource` ref，编辑器触发的 slash 菜单以输入框容器为锚点定位，按钮触发的仍以原 slash 按钮为锚点。
- **feat: 用户自定义预设系统（PresetManager）**：新增完整的用户预设 CRUD 体系：
  - **类型** (`types/index.ts`)：新增 `UserPreset`、`PresetNodeType`、`PresetTriggerMode` 及辅助常量
  - **IndexedDB** (`indexedDbService.ts`)：DB 升至 v4，新增 `presets` ObjectStore 和 CRUD
  - **服务层** (`fileService.ts`)：新增 `savePreset`/`loadPresets`/`deletePreset` 封装
  - **Store** (`useAppStore.ts`)：新增 `userPresets`、`presetManagerOpen` 状态及 CRUD actions + `initFromDb` 加载
  - **PresetManager** (新组件)：弹窗管理用户预设，支持按节点类型分 tab（文本/图像/视频/音频）、新建/删除/编辑预设、缩略图上传、contentEditable 提示词模板编辑器（含 `{{ 文章内容 }}` 占位 pill）、触发模式切换（直接触发 / 加入提示词）
  - **SlashCommandMenu**：新增 `userPresets` 和 `onManagePresets` props，菜单中展示用户预设列表（含缩略图），底部「管理预设」入口替换原「即将推出」占位
  - **样式** (`preset-manager.css`)：弹窗、tab、侧边栏列表、详情编辑区、footer 操作栏等完整样式
- **提交 `b7a053f`**：10 files, +1293/-51, feat: 用户自定义预设系统 + / 快捷命令菜单
- **优化 PresetManager 蒙版**：`.preset-modal-overlay` 蒙版背景加深（0.6→0.65），新增 `backdrop-filter: blur(6px)` 毛玻璃效果，增加淡入动画（`presetOverlayFadeIn`）。`.preset-modal--manager` 弹窗新增弹性缩放入场动画（`presetModalSlideIn`），scale 0.95→1 + translateY。
