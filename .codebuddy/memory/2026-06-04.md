# 2026-06-04

- **feat: 即梦 Dreamina 登录部分完善**: 将 SettingsPanel 中即梦卡片从纯占位 DOM（id 属性 + 无事件处理）重写为完整 React 响应式组件。新增 `DreaminaAuthData` 类型（`types/index.ts`），扩展 `AppConfig` 增加 `dreaminaAuth` 字段。登录流程：网页登录 / 扫码登录 → 浏览器打开即梦 → 用户粘贴 Cookie → 保存登录 → API 验证 → 持久化到 IndexedDB。支持已登录/未登录两套 UI（状态文字颜色、按钮切换）。新增 `openExternalUrl` 工具函数兼容 Tauri shell open / 浏览器 window.open。CSS 新增 `.dreamina-settings-status--logged-in` 已登录绿色状态。修改文件: `src/types/index.ts`, `src/components/SettingsPanel.tsx`, `src/styles/settings.css`

- **feat: 即梦登录改为 Tauri WebviewWindow 自动提取 Cookie（无需手动粘贴）**: Rust 端新增 `dreamina_login` 命令（`lib.rs`），创建 `WebviewWindow` 打开即梦登录页，轮询 URL 检测登录完成（离开 `/login` 路径），注入 JS 读取 `document.cookie` 并通过事件回传。前端在 Tauri 模式下调用 `invoke('dreamina_login', { mode })` 自动获取凭证；浏览器模式保留手动粘贴 Cookie。新增依赖：`tokio`（sync + time）、`url`。修改文件: `src-tauri/Cargo.toml`, `src-tauri/src/lib.rs`, `src/components/SettingsPanel.tsx`

- **fix: 即梦登录 Cookie 提取失败（SPA 模态框模式）**: 即梦是 SPA，`/login` 会立即重定向回首页并以模态框展示登录，导致 URL 检测在登录完成前就触发。改为注入 `DREAMINA_LOGIN_WATCHER` JS 脚本在 webview 中轮询 `document.cookie`（2s 间隔）+ MutationObserver 监听 DOM 变化（登录模态框关闭触发大量变更）+ 60s 兜底强制检查。Cookie 长度 > 80 字符且通过 `__TAURI__.event.emit` 回传即视为登录成功。修改文件: `src-tauri/src/lib.rs`

- **fix: 即梦登录窗口 event.emit 权限被拒**: Tauri 能力声明 `default.json` 的 `windows` 只含 `["main"]`，导致 `dreamina-login` webview 无法调用 `event.emit`。新增 `src-tauri/capabilities/dreamina-login.json`，为 `dreamina-login` 窗口单独授予 `core:event:default` + `core:event:allow-emit` 权限。

- **fix: 远程 webview 的 event.emit 仍被拒（local scope 限制）**: 即使加了 `dreamina-login.json` 能力声明，Tauri 2 的能力系统仍将其标记为 `local: true`，而 webview 加载的是远程页面。初次尝试改用 `document.title` 作为通信桥梁，但 Cookie 数据过长会被标题截断。

- **fix: document.title 截断问题 → 恢复 event.emit + remote URLs**: `document.title` 有长度限制，即梦的 Cookie 可能达数千字符会被截断。正确方案是在 Tauri 2 能力文件中添加 `"remote": {"urls": ["https://jimeng.jianying.com/**", "https://www.dreamina.com/**"]}` 字段，允许远程 URL 使用 `event.emit` IPC。恢复 event.emit 事件驱动方案（`oneshot::channel` + `Listener`），恢复 `dreamina-login.json` 能力文件，恢复 tokio `["sync", "time"]` features。修改文件: `src-tauri/src/lib.rs`, `src-tauri/Cargo.toml`, `src-tauri/capabilities/dreamina-login.json`（新建）。

- **refactor: 即梦登录整合 — 移除扫码登录，统一为网页登录**: 扫码登录和网页登录底层都打开 Dreamina 登录页，网页本身已支持手机号/扫码等多种方式。删除 `handleDreaminaQrLogin` 和 `handleDreaminaTauriLogin`，将 Tauri 登录逻辑内联到 `handleDreaminaWebLogin`。UI 上移除"扫码登录"按钮，更新描述文字。Rust 端 `dreamina_login` 命令去掉 `mode` 参数。登录 URL 统一改为 `https://jimeng.jianying.com/`。修改文件: `src/components/SettingsPanel.tsx`, `src-tauri/src/lib.rs`。cargo check 通过。

- **commit**: 已提交 `9f50461` feat: 即梦/Dreamina 登录功能 — Tauri WebviewWindow 自动提取 Cookie（11 files, +441/-49）

- **fix: 多选节点时隐藏浮动 Toolbar**: TextNode 和 ImageNode 的 `node-floating-toolbar` 原仅在 `selected` 时显示，现增加 `selectedNodeIds.length <= 1` 条件，选中多个节点时不显示。修改文件: `src/components/nodes/TextNode.tsx`, `src/components/nodes/ImageNode.tsx`。

- **fix: aiService.ts 变量重名**: 第 110 行 `resolvePromptToChatContent` 解构的 `content` 与第 154 行 API 响应的 `content` 重名导致 OXC 编译错误，将后者重命名为 `replyText`。

- **fix: 分组后点击组外节点漂移**: 根因是违反 xyflow/react 文档要求"父节点必须在 nodes 数组中出现在子节点之前"。原代码将 `groupNode` 追加到数组末尾（在所有子节点后），导致 React Flow 内部索引错乱，deselect 时触发错误位置重算。同时去掉了多余的 `import('@xyflow/react').then()` 异步延迟（`set()` 变为同步）。修改: `src/store/store.groups.ts`。

- **fix: 分组的复制粘贴/创建副本/删除全部存在问题**: 四个子问题一并修复：
  - (1) **复制(Ctrl+C)**: `copySelectedNodes` 只复制选中节点，不包含分组子节点和 `groups` 元数据 → 现在选中分组节点时 BFS 展开所有后代节点，同时复制对应的 `NodeGroup` 条目。
  - (2) **粘贴(Ctrl+V)**: `pasteNodes` 不处理 `parentId` 和 `groupId` 重映射，不创建新 `NodeGroup` → 现在通过 `idMap` 重映射所有关系（`parentId`、`data.groupId`、`groups.nodeIds`、`groups.id`），分组节点插入在子节点之前，并创建新 `NodeGroup` 条目。
  - (3) **创建副本(Ctrl+D)**: `handleDuplicate` 只深克隆单个节点 → 改为复用 `copySelectedNodes` + `pasteNodes`，自动处理分组级联。
  - (4) **删除(Del)**: 删除分组节点时不级联删除子节点，不清理 `groups` → `deleteNode` 和键盘批量删除现在 BFS 展开所有后代节点，级联删除文件/节点/边，同时更新 `groups`（删除已解散分组 + 从存活分组的 `nodeIds` 中移除已删除子节点）。
  - `clipboard` 类型从 `Node[]` 改为 `{ nodes: Node[]; groups: NodeGroup[] }`，所有引用处（`useKeyboardShortcuts.ts`, `useCanvasContextMenu.ts`, `Canvas.tsx`）同步更新。
  - 修改文件: `src/store/store.clipboard.ts`, `src/store/store.nodes.ts`, `src/hooks/useKeyboardShortcuts.ts`, `src/hooks/useNodeContextMenu.ts`, `src/hooks/useCanvasContextMenu.ts`, `src/components/Canvas.tsx`。

- **feat: 分组节点右键菜单增加「解除分组」选项**: 仅在 group 节点上右键时显示，调用已有的 `ungroupSelectedNodes()` 解散分组（移除 group 节点、子节点转绝对坐标、清理 NodeGroup 条目）。修改文件: `src/hooks/useNodeContextMenu.ts`（新增 `handleUngroup` + `ungroupSelectedNodes` 引用）、`src/components/canvas/NodeContextMenu.tsx`（新增可选 `onUngroup` prop + 过滤 `groupOnly` 菜单项 + `MENU_H` 调为 215）、`src/components/Canvas.tsx`（判断 `nodeId` 对应节点是否为 group 类型，条件传递 `onUngroup`）。
