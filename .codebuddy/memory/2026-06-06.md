# 2026-06-06

## SVG 属性 kebab-case → camelCase 修复

- `Sidebar.tsx` 两处 JSX 内 SVG 属性使用了 kebab-case，导致 React 警告
- line 380: `fill-rule` → `fillRule`，`clip-rule` → `clipRule`
- line 634: `stroke-linecap` → `strokeLinecap`，`stroke-linejoin` → `strokeLinejoin`，`stroke-width` → `strokeWidth`，`stroke-dasharray` → `strokeDasharray`，`stroke-dashoffset` → `strokeDashoffset`
- `MentionEditor.tsx` 和 `MattingEditor.tsx` 中是 `innerHTML` 原始字符串，kebab-case 正确，无需修改

## 用户自定义文件保存位置

- **需求**: 用户可以设置文件保存根目录，保存结构为 `{用户设置路径}/{项目ID}/**`
- **类型**: `AppConfig` 新增 `baseDataDir?: string` 字段
- **fileService.ts**:
  - 新增模块级 `_baseDataDir` 变量 + `setBaseDataDir()` 导出函数
  - `getProjectDataDir()` 优先使用 `_baseDataDir`，回退到 `appDataDir/data/`
- **store.config.ts**: `updateConfig`/`saveConfig`/`loadConfig` 中同步调用 `setBaseDataDir()`
- **SettingsPanel.tsx**:
  - 修复 `handleOpenProjectDir` 用 `shell.open()` 替代 `Command.create()`
  - 新增 `handleChooseBaseDir` 通过 `dialog.open({ directory: true })` 选择文件夹
  - UI 改为"文件保存位置"：根目录选择/显示 + 路径结构提示 + 当前项目目录 + 打开按钮

## shell.open() → Command 方案（最终方案）

### v1（失败）
- **尝试**: `tauri.conf.json` 中 `"shell": {"open": {"scope": [{"name": "**"}]}}` 
- **结果**: Panic — `ShellAllowlistOpen` 反序列化失败

### v2（capabilities scope，未生效）
- **尝试**: `capabilities/default.json` 中 `shell:allow-open` 自定义 regex 添加 file:// 匹配
- **结果**: 错误依旧 — 旧二进制缓存未重建，或 Tauri2 shell.open scope 机制复杂

### v3（最终方案：Command 方式）
- **回退**: 用 `Command.create('explorer', ['/root,', dir]).execute()` (Windows) / `Command.create('open'/'xdg-open', [dir]).execute()` (macOS/Linux)
- **优点**: `shell:allow-execute` 已在 capabilities 中配置好 explorer/open/xdg-open，无需任何额外 scope 配置
- **tauri.conf.json**: 保持 `"shell": {"open": true}` (恢复原样)
- **capabilities/default.json**: 保留 shell:allow-open 自定义 regex（无副作用）

### v4: 打开路径改为根目录
- **需求**: 按钮应打开用户设置的 `baseDataDir` 或系统默认目录，不带项目 ID
- **fileService.ts**: 新增 `getBaseDir()` — 返回 `_baseDataDir` 或 `{appDataDir}/data`
- **SettingsPanel.tsx**: `handleOpenProjectDir` 改用 `baseDataDir || getBaseDir()` 替代 `ensureProjectDataDir(projectId)`

## AI 生成文件本地持久化

- **需求**: 模型返回的在线 URL 下载到本地项目目录，原始 URL 保留到 `data-source-url` DOM 属性
- **types/index.ts**: 新增 `sourceUrl?: string` — 存储原始远程生成 URL
- **fileService.ts**: 
  - 新增 `extractFileNameFromUrl(url, fallbackPrefix)` — 从 ComfyUI `?filename=` 参数或普通 URL path 提取文件名
  - 新增 `downloadUrlAndSave(url, projectId, fallbackPrefix)` — fetch 下载 → `saveBinaryToProjectData` 本地持久化 → 返回 `{ filePath, assetUrl }`
- **AINodeDialog.tsx**: 图片/视频生成完成后，调用 `downloadUrlAndSave` 下载到本地；`imageUrl`/`videoUrl` 写入本地 `assetUrl`，`sourceUrl` 写入原始远程 URL，`filePath` 写入本地路径
- **ImageNode.tsx / VideoNode.tsx / AudioNode.tsx**: `<img>`/`<video>`/`<audio>` 添加 `data-source-url={data.sourceUrl}` 属性

## 编译修复（8 个 TS 错误）

- **Canvas.tsx:149**: `handleMove` event 参数类型从 `MouseEvent | TouchEvent` 改为 `MouseEvent | TouchEvent | null`（React Flow OnMove 允许 null）
- **AudioNode.tsx:132**: 删除未使用的 `PauseIcon` 组件
- **FreeAnglePanel.tsx:102**: 删除未使用的 `displayRotation` 变量
- **store.clipboard.ts:8**: 移除未使用的 `getNextDisplayId` 导入
- **store.clipboard.ts:46**: `pasteNodes` 未使用的 `position` 参数改为 `_position`
- **store.groups.ts:53,57**: `.map()` 回调中未使用的 `i` 参数移除
- **store.nodes.ts:69**: 移除未使用的 `groups` 解构
