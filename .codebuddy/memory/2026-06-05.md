# 2026-06-05

## 新增：本地图片上传到远端图床

**问题**：`aiService.ts` 中 `resolvePromptToChatContent()` 和 `resolvePromptWithImageRefs()` 直接将本地 `asset://localhost/...` 或 `data:...` 图片 URL 塞进发给远程 AI API 的请求体，远程 API 无法访问这些本地资源。

**改动**：

1. **新文件 `src/services/uploadService.ts`**：
   - `isLocalImageUrl(url)`: 判断是否为本地 URL（data: / asset:// / file://）
   - `uploadToRemote(url)`: 上传单张本地图片到远端图床，非本地 URL 直接返回；带内存缓存避免重复上传
   - `uploadAllToRemote(urls)`: 批量上传

2. **修改 `src/services/aiService.ts`**：
   - 新增 `resolveContentImageUrls(content)`: 处理 `generateText()` 中多模态 content 数组里的本地图片 URL
   - 新增 `resolveImageUrlArray(urls)`: 处理 `generateImage()` 中 `image_urls` 数组里的本地图片 URL
   - `generateText()`: 调用 `resolveContentImageUrls()` 后再发送
   - `generateImage()`: 调用 `resolveImageUrlArray()` 后再发送（包括 APIMart 异步路径）

**未改动**：`apimartService.ts` 角度控制流有自己的 `uploadToApimart()` 处理 data URL，asset URL 场景是已有问题，不在本次范围。

**数据流**：本地图片 → `uploadToRemote()` 用 APIMart `/uploads/images` 上传 → 公网 URL → 替换原 URL 发送给 AI API

## 修复：apimartService.ts 类型错误
- 第 171-172 行：`result` 类型标注为 `TaskResponse`，但 fallback `result.data ?? result` 导致 `task` 类型为 `TaskResponse | TaskData`，`TaskResponse` 无 `progress` 属性
- 修复：`result: TaskResponse | TaskData`，用 `'data' in result` 类型收窄确保 `task` 为 `TaskData`

## 修复：uploadService.ts 拿错 API Key
- `uploadToRemote()` 遍历 `config.providers` 取第一个有 key 的 provider，可能拿到非 APIMart 的 key（如 OpenAI）发给 `api.apimart.ai` 导致 invalid API key
- 修复：优先取 `config.providers.apimart` 的 key，APIMart 未配置时才降级到其他 provider

- **commit**: 已提交 `eeb818c` feat: AI 调用前自动上传本地图片到远端图床 + fix apimart 轮询响应解析（5 files, +206/-4）

## 代码精简优化（6 项，零风险）

### 新增共享模块
1. **`src/components/nodes/shared/useNodeRename.ts`**: 统一 hook，消除 TextNode/ImageNode/VideoNode/AudioNode 4 处的 `displayLabel + handleRename` 重复（~40 行）
2. **`src/components/nodes/shared/useSourceFileUpload.ts`**: 统一 hook，封装 `isUploading` 状态 + `uploadSourceFileToProject` 调用 + try/catch/finally 模板（~30 行），消除 ImageNode/VideoNode/AudioNode 3 处的重复
3. **`src/components/shared/ModalOverlay.tsx`**: 可复用 framer-motion 模态框外层容器（AnimatePresence + backdrop + 缩放动画），替换 SettingsPanel 的 ~30 行内联动画代码

### 组件精简
4. **SettingsPanel**: 合并 `ApiKeyInput`/`UrlInput` → 单一 `ConfigInput(type?)` 组件（~30 行），用 ModalOverlay 替换 AnimatePresence 内联代码
5. **ImageNode**: `blobToDataUrl` 内联代码 5 行 → 复用 `store.utils` 导出版本；移除未使用的 `currentProjectId` hook 订阅
6. **useKeyboardShortcuts**: 合并 Ctrl+S / Alt+S 两个 if 分支为单个条件判断

### 结果
- 净增 3 个共享模块（~80 行），净减 ~100 行重复代码
- TypeScript 编译通过，零 lint 错误
- 受影响文件：TextNode, ImageNode, VideoNode, AudioNode, SettingsPanel, useKeyboardShortcuts

## 根据测试报告优化（10/14 个问题已修复）
- **Canvas.tsx**: 9 处 `any` → 具体类型（`OnMove`/`EdgeChange[]`/`NodeChange<RFNode<BaseNodeData>>[]`/`GroupNodeDataAccess`）
- **SettingsPanel.tsx**: 删除 catch 块中未使用的 `err` 变量
- 剩余 4 个是 eslint 误报：setState in effect（2 处，异步模式）/ 事件回调内的 impure function（2 处）
