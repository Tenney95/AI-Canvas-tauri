- **MD 节点右键「打开文件所在位置」**：`useNodeContextMenu.ts` 的 `mediaTypes` 数组新增 `'ai-markdown'`
- **SplashScreen 点击重置修复**：先加 `pointer-events-none`（仍穿透），改为 `onClick` + `onMouseDown` 都 `stopPropagation()`
- **全屏显示功能重构 + MD 节点全屏**：
  - 新建 `src/components/shared/FullscreenOverlay.tsx`：framer-motion 动画蒙层（AnimatePresence + 弹簧缩放）
  - Escape 关闭内置在组件中
  - 新建 `src/styles/nodes.css` fullscreen CSS 区块（`.fullscreen-overlay/.panel/.header/.body`）
  - `TextNode.tsx` 重构：替换旧 `createPortal` 全屏为 `<FullscreenOverlay>`
  - `MarkdownNode.tsx` 新增全屏：预览模式默认打开，工具栏可切换编辑/预览
  - Tauri overlay 偏移规则新增 `.fullscreen-overlay`
- **全屏蒙层统一替换 + ImageNode 全屏**：
  - `FullscreenOverlay` 新增 `hideHeader`（隐藏标题栏，关闭按钮绝对定位）+ `bodyClassName` props
  - `PanoramaNode` 删除自建 `PanoramaFullscreenModal`（~95行），替换为 `<FullscreenOverlay hideHeader bodyClassName="fullscreen-body--pano">`
  - `PanoramaNode` 底部截图/下载按钮改用 `AnimatedButton`
  - `ImageNode` 新增全屏预览：`<FullscreenOverlay bodyClassName="fullscreen-body--image">` + `<img class="fullscreen-img-view">`
  - `ImageNodeToolbar` 的 `onFullscreen`/`onDownload` 已接入
  - `nodes.css` 新增 `.fullscreen-close--absolute` / `.fullscreen-body--pano` / `.fullscreen-body--image` / `.fullscreen-img-view` / `.fullscreen-pano-toolbar`
  - `nodes-panorama.css` 清理旧全屏模态 CSS（`.panorama-fullscreen-modal` 等），保留 `.pano-fs-btn` 按钮样式
  - 现在所有节点的全屏功能统一使用 `FullscreenOverlay`（Text / Markdown / Panorama / Image，共4个）
