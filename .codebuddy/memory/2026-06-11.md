- **MD 节点右键「打开文件所在位置」**：`useNodeContextMenu.ts` 的 `mediaTypes` 数组新增 `'ai-markdown'`
- **SplashScreen 点击重置修复**：先加 `pointer-events-none`（仍穿透），改为 `onClick` + `onMouseDown` 都 `stopPropagation()`
- **全屏显示功能重构 + MD 节点全屏**：
  - 新建 `src/components/shared/FullscreenOverlay.tsx`：framer-motion 动画蒙层（AnimatePresence + 弹簧缩放）
  - Escape 关闭内置在组件中
  - 新建 `src/styles/nodes.css` fullscreen CSS 区块（`.fullscreen-overlay/.panel/.header/.body`）
  - `TextNode.tsx` 重构：替换旧 `createPortal` 全屏为 `<FullscreenOverlay>`
  - `MarkdownNode.tsx` 新增全屏：预览模式默认打开，工具栏可切换编辑/预览
  - Tauri overlay 偏移规则新增 `.fullscreen-overlay`
- **全屏蒙层统一替换 + ImageNode 全屏**：
  - `FullscreenOverlay` 新增 `hideHeader`（隐藏标题栏，关闭按钮绝对定位）+ `bodyClassName` props
  - `PanoramaNode` 删除自建 `PanoramaFullscreenModal`（~95行），替换为 `<FullscreenOverlay hideHeader bodyClassName="fullscreen-body--pano">`
  - `PanoramaNode` 底部截图/下载按钮改用 `AnimatedButton`
  - `ImageNode` 新增全屏预览：`<FullscreenOverlay bodyClassName="fullscreen-body--image">` + `<img class="fullscreen-img-view">`
  - `ImageNodeToolbar` 的 `onFullscreen`/`onDownload` 已接入
  - `nodes.css` 新增 `.fullscreen-close--absolute` / `.fullscreen-body--pano` / `.fullscreen-body--image` / `.fullscreen-img-view` / `.fullscreen-pano-toolbar`
  - `nodes-panorama.css` 清理旧全屏模态 CSS（`.panorama-fullscreen-modal` 等），保留 `.pano-fs-btn` 按钮样式
  - 现在所有节点的全屏功能统一使用 `FullscreenOverlay`（Text / Markdown / Panorama / Image，共4个）
  - **修复**：全景全屏不显示 → 经历三轮调试：
    1. `.fullscreen-panel` 无确定高度，body 高度为 0 → 加 `min-height: 80vh`，body 有高度了
    2. 但 panorama 仍不显示：`min-height` 不是确定尺寸，flex 子级 `flex:1`（basis:0%）无法分配空间 → Three.js canvas 0 高度
    3. 尝试 `height:82vh` 失败：`height` + `flex:1`（basis:0%）+ `overflow-y:auto` 三者冲突，高度又消失
    4. **最终方案**：`.fullscreen-body--pano` 从 flex 改为 **CSS Grid** (`grid-template-rows: 1fr auto`)，Grid 的 `1fr` 天然支持 `min-height` 约束的容器，不依赖确定高度，wrapper 正确拿到空间
- **fullscreen-toolbar 移入 header**：
  - `FullscreenOverlay` 新增 `headerContent?: ReactNode` prop，渲染在标题栏中（标题与关闭按钮之间）
  - `MarkdownNode` 全屏的编辑/预览切换按钮从 body 移到 `headerContent`
  - CSS：`fullscreen-toolbar` 去掉冗余 padding，新增 `.fullscreen-header-extra` 包装器
- **文件删除改为回收站**：
  - Rust 端：`Cargo.toml` 新增 `trash = "5"` crate，`lib.rs` 新增 `move_to_trash` Tauri command（调用 `trash::delete(path)`，支持文件和目录）
  - 前端 `fileService.ts`：
    - 新增 `moveToTrash()` 函数，调用 `invoke('move_to_trash', { path })`
    - `deleteNodeFile()` → 改用 `moveToTrash(fp)`（节点关联文件）
    - `deletePermanentFile()` → 改用 `moveToTrash(filePath)`（AssetsPanel 手动删除）
    - `removeDirRecursive()` → 简化为直接 `invoke('move_to_trash')` 整个目录（不再逐文件递归）
    - `deleteFile()` 保留（底层 `remove()`）作为永久删除备选
    - 清理了未使用的 `DirEntry` type import
  - 所有文件删除（节点删除、项目目录删除、永久文件删除）统一走系统回收站；Windows→回收站、macOS→废纸篓、Linux→FreeDesktop Trash
  - 全量构建通过：TS 编译零错误，Rust 编译通过，打包生成 MSI + NSIS 安装包
- **SplashScreen 拖拽修复**：
  - 外层 wrapper 零高度导致子元素不显示 → 把 `data-tauri-drag-region` 合并到 `fixed inset-0` div
  - `onMouseDown stopPropagation()` 阻断拖拽 → 拆分为两层：外层纯拖拽（不拦截事件），内层 `stopPropagation` 防穿透
- **快捷键撤销/重做 Bug 修复**：
  - Tauri 全局快捷键 `CommandOrControl+Z/Y` 与 JS `keydown Ctrl+Z/Y` 同时触发，每次撤销/重做实际执行 2 步 → 从 `GLB_SHORTCUTS` 移除 Z/Y，只保留 JS 层
  - 键盘 Delete 先 `commit`→删边→`commit`→删节点，两次提交导致撤销先回到「无边有节点」中间态 → 合并为单次 `commitToHistory()` + 单次 `setState()`
- **撤销/重做支持文件还原**：
  - 问题：删除节点后文件进了系统回收站，撤销只恢复画布状态（节点/边），文件回不来
  - 方案：引入 undo-trash 暂存机制（不直接进系统回收站）
  - `fileService.ts`：
    - 新增 `undoTrashMap`（originalPath → trashPath）+ `getUndoTrashDir()` / `moveToUndoTrash()` / `restoreFromUndoTrash()` / `flushUndoTrashDirs()`
    - `moveToUndoTrash()`：文件 copy→`.trash/` 子目录 → delete 原文件；失败回退走系统 `moveToTrash()`
    - `restoreFromUndoTrash()`：从 `.trash/` copy 回原路径 → delete 暂存文件
    - `deleteNodeFile()` → 改用 `moveToUndoTrash()`（`deletePermanentFile` 保持走系统回收站）
  - `store.history.ts`：
    - `undo()`：恢复快照后遍历所有节点，fire-and-forget `restoreFromUndoTrash(filePath)`
    - `redo()`：比较 pre/post 节点集，找到被移除的节点，fire-and-forget `moveToUndoTrash(filePath)`
  - `.trash/` 暂存文件在项目删除时随项目目录一同进系统回收站（`removeDirRecursive` 已走 `move_to_trash`）
