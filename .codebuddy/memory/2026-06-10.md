- 新增「打开文件所在位置」右键菜单功能：4 个文件改动（fileService / useNodeContextMenu / NodeContextMenu / Canvas）
- 修复定位错误（第 2 轮）：`tauri.conf.json` 中 `scope` 字段报错 `unknown field` — Tauri 2 的 shell 命令 scope 在 `capabilities/default.json` 中配置，不在 `tauri.conf.json`；macOS `Command.create` name 恢复为 `mac-open`（匹配 capability 映射）
- 全景图节点右键也加上「打开文件所在位置」
- ModalOverlay z-50 → z-[250]：修复 ai-dialog-float(z-200) / wf-panel(z-201) 遮挡 SettingsPanel 的问题
- SettingsPanel 重构：将 API Key 配置部分（厂商卡片、即梦登录、通用模型、服务地址、测试连接）抽离为独立组件 `src/components/settings/ApiKeySettings.tsx`
- 通用模型点击「添加模型」后自动 `saveConfig()` 持久化
- **全链路联动：通用模型 → 选择器 → API 调用**：
  - `types/index.ts`：新增 `CATEGORY_TO_NODE_TYPES` 映射（GeneralModelCategory → NodeType[]）
  - `ModelSelector.tsx`：从 store 读取 `generalModels`，动态生成「通用模型」分组（默认展开），按 `nodeType` 过滤
  - `aiService.ts`：新增 `resolveGeneralModel`、`parseMultiPathResponse`、`executeGeneralAsyncTask`、`parseGeneralTextResponse`、`parseGeneralImageResponse` 等辅助函数
  - `generateText`：通用模型走 `/chat/completions`，灵活解析多字段（choices[0].message.content / data.content / data.text）
  - `generateImage`：通用模型走 `/images/generations`，灵活解析（data[0].url / result.images[0]）
  - `generateVideo` / `generateAudio`：通用模型走异步任务轮询（提交 → task_id → 轮询 /tasks/{id} → 多路径解析）
- **合并 localllm → 通用模型**：删除 `localLLMUrl` 字段、localllm 模型分组、独立配置 UI，超时机制迁移至 general provider。旧数据兼容错误提示。
- **通用模型 API Key 校验放宽**：添加模型时去掉 API Key 必填校验，调用路径 4 处长去掉 `!gm.apiKey` 强制报错，改为 `gm.apiKey || ''`。Ollama 等本地模型可直接 `http://localhost:11434/v1` + 空 Key 接入。
- **新增 Markdown 源节点 (`ai-markdown`)**：编辑/预览切换、上传 .md 文件、保存到本地项目目录（`saveBinaryToProjectData`）、拖拽调整大小、Handle 连线。紫色 MD 标签。注册到 Canvas/右键菜单/侧边栏/连线菜单。
- **修复 MarkdownNode 编译错误**：import 路径 `./shared/` → `../shared/`；`AnimatedButtonProps` 改为 `Omit<MotionProps, 'onClick'>` 解决 onClick 类型冲突，新增 `title` prop；删除未使用变量 `selectedNodeIds`。
- **提交 `09df955`**：18 files, +442/-104。
- **MarkdownNode 预览渲染**：因 npm 不可用，放弃 `marked` 依赖，手写轻量级 `renderMarkdown()` 到 `src/utils/renderMarkdown.ts`，支持标题/粗体/斜体/删除线/代码块/行内代码/列表/引用/链接/图片/水平线。预览模式不再显示原始 `#` 等标记符号。从 `package.json` 移除 `marked`。
- **MarkdownNode 编辑拖拽修复**：textarea 添加 `nodrag nowheel` class，禁止被 React Flow 拦截指针事件，可正常拖选文本。
- **Minimap 颜色修复**：`ai-text` 从未定义的 `--brand-hover` → `--node-text-light`；新增 `ai-markdown` 紫色（`--node-markdown`/`--node-markdown-light`/`--node-markdown-bg`）。
- **画布选框改为 Partial 模式**：`selectionMode={SelectionMode.Partial}`，节点部分进入选框即选中。
- **Markdown 节点排除 AI 生成弹窗**：`Canvas.tsx` `onNodeClick` 排除 `ai-markdown`；`useConnectionDropMenu.ts` `CONNECTION_MENU_MAP` 中移除所有 `Markdown 文档` 选项，`ai-markdown` 来源节点菜单为空数组。
- **NodeLabel 图标替换**：从单字母文本（T/I/V/A/P/MD）替换为 `@iconify/react` Material Design Icons（`mdi:text-box-outline`/`mdi:image-outline`/`mdi:video-outline`/`mdi:volume-high`/`mdi:panorama`/`mdi:language-markdown-outline`）。
- **画布背景主题系统**：新增3种背景可选（default暗色/solar-system太阳系/minimal极简纯黑），配置持久化到 `canvasBackground`。
  - 新增类型 `CanvasBackground`（`src/types/index.ts`）
  - 新增 `src/components/backgrounds/CanvasBackground.tsx`：背景路由组件，导出 `BACKGROUND_OPTIONS`
  - 新增 `src/components/backgrounds/SolarSystemBackground.tsx`：太阳系主题背景（行星轮播、星场动画、弧形导航），改编自 CodePen
  - 新增 `src/styles/backgrounds.css`：背景样式
  - `App.tsx`：app-box 内首个子元素渲染 `<CanvasBackground />`
  - `SettingsPanel.tsx` > 常规标签页：新增「画布背景」3 列卡片选择器，带缩略预览
  - 行星图片从 CDN JPG 改为本地 `src/assets/images/bg/{planet}.png`（Vite import 静态导入）
  - 删除行星名称叠加层（`.solar-planet-overlay`）和底部弧形导航（`.solar-nav`），纯视觉背景
  - 简化 `PLANETS` 数组为 `PLANET_IDS = Object.keys(PLANET_IMAGES)`
- **提交 `cbaa735`**：23 files, +583/-91，画布背景系统 + SolarSystem 背景 + Markdown 渲染 + 样式优化
