# 2026-05-28

## SettingsPanel API Key 改造
- 按照新版 UI 重写了 `SettingsPanel.tsx` 的 API Key 设置页面
- 为每个厂商提供独立卡片：APIMart、火山方舟、RunningHUB、GRSAI、派欧云(PPIO)、AICanvas、OpenAI兼容格式、即梦
- 每个卡片包含：厂商标识(badge/icon)、标题、状态标签、余额显示、测试连接按钮、获取 Key 按钮、API 密钥输入
- RunningHUB 支持双密钥（工作流 API 密钥 + 模型 API 密钥），分别存储为 `runninghub` 和 `runninghub-model`
- OpenAI 兼容格式支持接口地址 + API 密钥双输入
- 即梦(Dreamina) 支持网页登录/扫码登录/退出登录按钮
- 保留"服务地址"卡片（本地大模型 + ComfyUI）
- 底部操作栏：测试连接 + 保存按钮

## Store 改动
- `useAppStore.ts` 新增 `setProviderUrl(providerName, url)` action — 设置厂商 baseUrl
- `useAppStore.ts` 新增 `setProviderConfig(providerName, cfg)` action — 批量设置厂商配置

## 样式
- 新增 `src/styles/settings.css` — settings-card、settings-provider-status、settings-provider-balance、settings-provider-test-btn、settings-getkey、settings-hint-tooltip 等样式
- `src/index.css` 新增 @import settings.css

## 外部链接打开支持（已修复 2 次）
- 点击"获取 Key"按钮现在通过 `@tauri-apps/plugin-shell` 的 `open()` 打开外部网页
- 新增依赖：Cargo `tauri-plugin-shell = "2"` + npm `@tauri-apps/plugin-shell: "^2"`
- `lib.rs` 注册 `tauri_plugin_shell::init()`
- `tauri.conf.json` 配置 `plugins.shell.open = true`
- `SettingsPanel.tsx` 的 `GetKeyButton` 使用 `<a>` 标签 + onClick：
  - 始终 preventDefault，然后尝试 Tauri shell open()
  - Tauri shell 不可用或失败时 fallback 到 `window.open()`
- **关键修复**：`capabilities/default.json` 必须添加 `shell:default` 和 `shell:allow-open` 权限，否则 Tauri 安全策略会拦截 open() 调用且无任何报错

## TextNode 浮动工具栏
- 为已有文本输出的选中 TextNode 添加浮动工具栏，出现在节点上方
- 三个按钮：复制（带 ✓ 反馈动画）、清除空行（3+ 连续空行→2 个空行）、全屏显示
- 复制功能使用 `navigator.clipboard.writeText` + `useCopyFeedback` hook（1.5s 视觉反馈）
- 全屏显示：fixed overlay 蒙层 + 居中面板，Escape 或点击蒙层关闭
- 工具栏仅在 `selected && hasOutput` 时渲染
- 按钮 click 均调用 `e.stopPropagation()` 防止 React Flow 拦截
- CSS：`nodes.css` 新增 `.text-toolbar`（bottom: calc(100%+6px) 定位在节点上方）、`.text-fullscreen-*` 系列样式

## TextNode 全屏编辑改造
- **删除**了 TextNode 的原有双击编辑功能（`isEditing`、`editValue`、`handleDoubleClick`、`handleEditSave`、`handleEditKeyDown`、`handleContentClick`）
- 全屏 overlay 中的 body 从只读 `<div>` 改为 `<textarea>`（`.text-fullscreen-editor`）
- 全屏编辑器通过 `onChange` 实时写回 `data.output`（`updateNodeData(id, { output: e.target.value })`）
- 打开全屏时自动聚焦 textarea 并将光标移到末尾
- 节点预览区域简化为纯 `<div>` 只读展示，去掉了 `data-tooltip="双击编辑"`

## TextNode 全屏 overlay — Portal 修复
- **问题**：全屏 overlay 渲染在 React Flow 节点的 DOM 树内部，被节点的 `overflow`/层叠上下文约束，尺寸被限制在节点范围内
- **修复**：使用 `createPortal(..., document.body)` 将全屏 overlay 传送到 body 下，完全脱离节点 DOM 约束
- `TextNode.tsx` 新增 `import { createPortal } from 'react-dom'`，

## 全局滚动条美化
- `base.css` 重写 `::-webkit-scrollbar-*` 样式：
  - thumb 颜色 `#3a3a50`、`border-radius: 9999px`、`transition: background 0.2s`
  - hover: `#555570`、active: `#6b6b88`
  - 新增 `scrollbar-corner` 透明处理
- 新增 Firefox 兼容：`scrollbar-width: thin; scrollbar-color: #3a3a50 transparent`
- 通过 `*` 选择器全局应用
- 后续改为 4px（更细）

## 视频 ComfyUI 工作流调用
- **问题**：视频节点选择 ComfyUI 工作流后点生成，走的是 `generateText()` 分支，因 comfyui 无 API Key 报错
- **修复**：`aiService.ts` 新增视频 ComfyUI 调用链路，和图片 ComfyUI 共用 `getComfyUIConfig`、`submitComfyUIWorkflow`、`promptComfyUIWorkflow`
  - 新增 `AIVideoGenParams` 接口
  - 新增 `injectVideoParamsIntoWorkflow()` — 注入 videoResolution/videoFps/videoFrames 到工作流 JSON
  - 新增 `pollComfyUIHistoryForVideo()` — 轮询时查找 `videos`/`gifs`/`images` 输出
  - 新增 `executeComfyUIVideoGenerate()` 和导出 `generateVideo()`
  - 重构原 `executeComfyUIGenerate`：提取 `submitComfyUIWorkflow` + `promptComfyUIWorkflow` 为共享函数，消除重复代码
- `AINodeDialog.tsx` 新增 `ai-video` 分支，调用 `generateVideo()`，返回结果存入 `videoUrl`/`thumbnailUrl`/`output`

## TextNode 有内容时禁用 prompt-panel
- `Canvas.tsx` 的 `onNodeClick` 新增判断：`node.data?.type === 'ai-text' && node.data?.output` 时直接 return，不弹出 prompt 对话框
- 已有文本输出的节点点击只会选中/显示工具栏，不再弹出输入面板

## ComfyUI 工作流：只替换 @提及节点的值
- **问题**：调用 ComfyUI 工作流时，`injectPromptsIntoWorkflow`/`injectDimensionsIntoWorkflow`/`injectVideoParamsIntoWorkflow` 无差别遍历所有 IO 节点替换值，未 @ 的节点也被覆盖
- **修复**：
  - `injectPromptsIntoWorkflow`：从遍历所有 `ioNodeIds` 改为只遍历 `workflowInputs` 的 keys（被 @ 的节点）
  - `injectDimensionsIntoWorkflow`：新增 `mentionedNodeIds` 参数，有指定时只修改被 @ 的节点；无指定则全量注入（兼容旧行为）
  - `injectVideoParamsIntoWorkflow`：同上，新增 `mentionedNodeIds` 参数
  - 调用方 (`executeComfyUIGenerate`/`executeComfyUIVideoGenerate`) 传入 `workflowInputs ? Object.keys(workflowInputs) : undefined`
- 未 @ 的工作流节点保留原始 JSON 中的值不动

## ComfyUI Workflow 图片上传
- **需求**：@ 出 image 类型 IO 节点（LoadImage）时，将画布图片节点的图片上传到 ComfyUI 服务器，替换工作流 JSON 中 LoadImage 节点的 `inputs.image`
- **实现**（`aiService.ts`）：
  - `uploadImageToComfyUI(baseUrl, imageUrl)` — 支持 data URL 和远程 URL，上传到 `POST /upload/image`，返回 `{ name, subfolder, type }`
  - `injectImagesIntoWorkflow()` — 遍历 `workflowInputs`，找出 image 类型 IO 节点，resolve @ 引用得到图片 URL，upload，设置 `inputs.image = name` + `inputs.upload = 'image'`
  - `submitComfyUIWorkflow` 在 prompt 注入后调用 `await injectImagesIntoWorkflow()`
  - 新增 `import type { WorkflowIONode } from '../types'`

## Canvas 右键上下文菜单 + 剪贴板
- **需求**：右键画布空白位置弹出三级上下文菜单（添加节点 → 生成节点/源节点 → 具体类型）
- **实现**：
  - `useAppStore.ts`：新增 `clipboard` 状态 + `copySelectedNodes()` + `pasteNodes(position)` actions
  - `useKeyboardShortcuts.ts`：新增 Ctrl+C 复制选中节点、Ctrl+V 粘贴（依赖新增：`copySelectedNodes`/`pasteNodes`）
  - `Canvas.tsx`：
    - `onContextMenu` 只在 `.react-flow__pane`（画布空白）触发，显示菜单
    - 三级菜单状态通过 `hoverMenu: 'addNode' | 'genNode' | 'srcNode' | null` 管理
    - 子菜单 hover 展开带 120ms 延迟，离开父行时通过 timer 清除
    - 菜单项：添加节点 ▶、粘贴 Ctrl V、撤销 Ctrl Z、重做 Ctrl Y
    - 添加节点 → 生成节点 ▶（生成文本/图像/视频/音频）、源节点 ▶（源文本/图像/视频/音频）
    - 粘贴按钮在 clipboard 为空时 disabled（opacity: 0.35）
    - 点空白或 Escape 关闭菜单
  - CSS：新增 `canvas-context.css`，`index.css` 新增 @import
- **节点类型**：生成节点和源节点当前均映射到相同的 `ai-text/ai-image/ai-video/ai-audio` 类型，未来可按 subtype 区分

## 右键菜单子菜单 hover 修复
- **问题**：鼠标从父菜单行移到子菜单面板时，子菜单立即消失
- **根因**：
  1. Level 1 → Level 2：`onMouseLeave` 立刻清除 120ms 定时器，子菜单还没渲染就被掐掉
  2. Level 2 → Level 3：`onMouseLeave={() => openSubmenu(null)}` 立即将 hoverMenu 设为 null，所有子菜单消失
- **修复**：改为 `showSubmenu()`（立即显示 + 取消隐藏定时器）和 `hideSubmenu(backTo)`（250ms 延迟后回退到 `backTo`）
  - 所有 `onMouseEnter` → `showSubmenu(menu)`（立即展开，取消任何待执行的隐藏）
  - 所有 `onMouseLeave` → `hideSubmenu(backTo)`（250ms 后回退，给光标跨间隙留足时间）
  - 进入子菜单面板时 `showSubmenu` 会取消父行的 hideSubmenu 定时器
- **再次修复**：Level 2 子菜单的显示条件从 `hoverMenu === 'addNode'` 改为 `hoverMenu === 'addNode' || 'genNode' || 'srcNode'`，解决 hover 到 Level 3 时 `hoverMenu` 变为 `genNode/srcNode` ≠ `addNode` 导致 Level 2 消失
- Level 3 条件也同步简化：去掉冗余的 `|| 'addNode'` 保护（Level 2 现已始终可见）

## 源节点 vs 生成节点 — 右键菜单完善
- **需求**：源节点是去掉 AI 对话框的生成节点，右上角多上传按钮，标题显示"粘贴文本/图像/视频/音频"，上传后标题显示文件名，节点大小根据内容更新
- **实现**：
  - `types/index.ts`：`BaseNodeData` 新增 `role?: 'generator' | 'source'` + `fileName?: string` 字段
  - `services/fileService.ts`：新增 `uploadSourceFile(accept?)` 函数，返回 `{ dataUrl, fileName, fileSize }`，支持 Tauri 和浏览器双模式
  - `components/Canvas.tsx`：
    - `SRC_NODE_ITEMS` 标题改为"粘贴文本/图像/视频/音频"
    - `addNodeAtCtxPos` 新增第三个参数 `role`，默认 `'generator'`
    - 源节点创建时不设 `imageSize/aspectRatio`（不需要画质选择）
    - `onNodeClick` 跳过 `role === 'source'` 的节点（不弹 AI 对话框）
  - `components/nodes/shared/NodeLabel.tsx`：新增 `showUpload` + `onUpload` props，右上角渲染上传按钮（SVG 上传图标），标签文字添加 `truncate` 防止溢出
  - `components/nodes/TextNode.tsx`：源节点模式 — 浮动工具栏不显示、上传 `.txt/.md/.json/.csv` 等文本文件、上传后用 `TextDecoder` 解码 UTF-8 内容存入 `output`、根据行数估算 `nodeHeight`、标题改为文件名
  - `components/nodes/ImageNode.tsx`：源节点模式 — 上传图片文件、`img.onload` 获取实际尺寸后按比例计算 `nodeHeight`、标题改为文件名
  - `components/nodes/VideoNode.tsx`：源节点模式 — 上传视频文件 `.mp4/.webm/...`、标题改为文件名
  - `components/nodes/AudioNode.tsx`：源节点模式 — 上传音频文件 `.mp3/.wav/...`、标题改为文件名、不显示 Beta 标签
  - `styles/nodes.css`：新增 `.node-upload-btn` 样式（22x22, frosted glass hover）
- **节点尺寸更新**：
  - TextNode 源节点：根据文本行数 `Math.max(120, Math.min(600, 40 + lineCount * 20))`
  - ImageNode 源节点：`img.onload` → `nodeWidth / (naturalWidth / naturalHeight)` → `Math.max(120, previewHeight + 36)`

## 图片上传后尺寸修正
- **问题**：上传图片后节点尺寸不按比例变化
- **根因**：高度计算公式 `nodeWidth - 24` 和 `+ 36` 错误
  - padding 补偿应是 `nodeWidth - 4`（`.image-node .node-preview.compact` 的 padding 为 2px 两侧）
  - `+ 36` 不该加（NodeLabel 是绝对定位，不在 body div 内）
- **修正**：`contentWidth = nodeWidth - 4; previewHeight = round(contentWidth / naturalRatio); newHeight = max(120, previewHeight + 4)`
- **效果**：16:9 横图约 159px（近默认 158px，变化极微），方形约 280px，9:16 竖图约 495px

## 右键菜单点击无效修复
- **问题**：右键菜单中点击生成节点/源节点不创建节点
- **根因**：`closeCtxMenu` 用 `mousedown` 事件监听，但只检查 `ctxMenuRef.current.contains(target)`，Level 2/3 子菜单是独立 DOM 节点不在 `ctxMenuRef` 内 → `mousedown` 时判定为"外部点击"立即关闭菜单 → `click` 事件永远触发不了
- **修复**：`contains` 检查增加三个条件（任一满足则不关闭）：
  1. `ctxMenuRef.current.contains(target)` — Level 1 菜单内
  2. `ctxSubmenuRef.current.contains(target)` — Level 2 子菜单内
  3. `target.closest('.v2-canvas-ctx-menu')` — 任意层级子菜单内（泛用覆盖）

## 上传按钮移到 node-preview 右上角
- 从 `NodeLabel` 组件中移除 `showUpload`/`onUpload` props 和上传按钮渲染
- 在 ImageNode / VideoNode / AudioNode 的 `.node-preview.compact` 内直接渲染上传按钮（仅 `isSource` 时显示）
- CSS：`.node-upload-btn` 改为 `position: absolute; top: 6px; right: 6px`，尺寸加大到 28x28，加了 `backdrop-filter: blur(6px)` 半透明背景
- 遗漏修复：TextNode 也同步移除 NodeLabel 的 `showUpload`/`onUpload`，并在 `.node-preview.compact` 内加入上传按钮

## 源节点显示 displayId
- **改动**：所有节点组件（Image / Video / Audio / Text）的 `NodeLabel` 中移除 `isSource ? undefined :` 条件
- `addNode` 已经给所有节点分配 `displayId`，源节点也应显示编号

## displayId 改为扫描已有节点最大值 +1
- 移除 `nextNodeDisplayId` 状态字段和初始值 `10`
- 新增 `getNextDisplayId(nodes)`：遍历所有节点的 `displayId`，取 `max + 1`
- `addNode` / `addNodeWithEdge` / `pasteNodes` 均改用 `getNextDisplayId` 计算编号
- 好处：删除节点后编号不会跳号，粘贴时也基于现有最大 ID 递增

## react-dev-inspector 安装配置
- 安装了 `react-dev-inspector` v2.0.1（devDependencies），支持点击页面元素跳转到 IDE 代码行
- **vite.config.ts**：导入 `inspectorServer` 必须用 `@react-dev-inspector/vite-plugin`（v2 废弃了 `react-dev-inspector/plugins/vite`）
- **src/App.tsx**：用 `<Inspector keys={['control', 'shift', 'c']} disableLaunchEditor={false}>` 包裹，仅 DEV 模式启用
- 快捷键 Ctrl+Shift+C 进入 Inspect 模式，悬停高亮组件边界，点击跳转 VS Code
- 改用 `onInspectElement` 回调 + `codebuddycn://file/` URL scheme，使点击跳转到 CodeBuddy CN 而非标准 VS Code
- **修复 onInspectElement 不触发**：`@vitejs/plugin-react` v6 用 OXC 不注入 `__source`，导致 `fiber._debugSource` 为 null
  - 新增 `@rolldown/plugin-babel` + `@babel/plugin-transform-react-jsx-source`，在 OXC 前用 Babel 注入 `__source`
  - Babel 插件仅 dev 模式启用，仅处理 .tsx/.jsx 文件，只注入元数据不转译 JSX
  - 插件顺序：`babel(__source 注入)` → `react(OXC JSX 转译)` → `inspectorServer()`
  - **修复 `babelHelpers` 报错**：`@rolldown/plugin-babel` 不支持 `babelHelpers: "bundled"`（那是 `@rollup/plugin-babel` 的 API），已移除该选项
- **提交**：commit `c721793`，18 files changed, 3034+ / 81-

## 生成图片/视频节点有内容后不再弹对话
- **Canvas.tsx** `onNodeClick`：新增两条判断
  - `node.data?.type === 'ai-image' && node.data?.imageUrl` → return（不弹对话框）
  - `node.data?.type === 'ai-video' && node.data?.videoUrl` → return（不弹对话框）
- 与文本节点现有逻辑一致：有输出内容的生成器节点点击时不再弹 PromptPanel

## Canvas.tsx 重构：抽离组件和 Hooks（895 → ~270 行）

### 新增文件
| 文件 | 类型 | 职责 |
|---|---|---|
| `hooks/useConnectionDropMenu.ts` | Hook | 连线拖拽未命中弹出的"引用该节点生成"菜单：状态、click-outside/Escape 关闭、handleConnectEnd/handleSelect |
| `hooks/useCanvasContextMenu.ts` | Hook | 三级级联右键菜单：状态、hover 延迟、click-outside 关闭、添加节点/撤销/重做/粘贴动作 |
| `components/canvas/CanvasToolbar.tsx` | 组件 | 底部面板：网格开关、连线类型切换、缩放滑块 |
| `components/canvas/ConnectionMenu.tsx` | 组件 | 连线拖拽菜单 UI：header + 动态菜单项（带图标和颜色） |
| `components/canvas/CanvasContextMenu.tsx` | 组件 | 三级级联右键菜单 UI：Level1 → Level2（生成/源节点）→ Level3（具体类型） |
| `components/canvas/CanvasEmptyState.tsx` | 组件 | 空画布提示页 + 快捷创建按钮 |

### 修改文件
- `Canvas.tsx`：移除所有内联的菜单状态/effect/JSX、`CONNECTION_MENU_MAP`、`GEN_NODE_ITEMS`、`SRC_NODE_ITEMS`、空状态 JSX、toolbar JSX
- `onNodesChange` 内联函数提取为 `handleNodesChange` useCallback

## 节点右键菜单

### 新增文件
| 文件 | 类型 | 职责 |
|---|---|---|
| `hooks/useNodeContextMenu.ts` | Hook | 节点右键菜单状态：可见/位置/nodeId、click-outside/Escape 关闭、复制/剪切/副本/删除动作 |
| `components/canvas/NodeContextMenu.tsx` | 组件 | 节点右键菜单 UI：复制(Ctrl C)、剪切(Ctrl X)、创建副本(Ctrl D)、删除(Del)，删除项红色危险样式 |

### 修改文件
- `Canvas.tsx`：新增 `onNodeContextMenu={openNodeCtxMenu}`、渲染 `NodeContextMenu`
- `styles/canvas-context.css`：新增 `.v2-menu-row-danger` 红色危险按钮样式

### 功能要点
- **复制**：选中节点 → `copySelectedNodes` → 存入 clipboard
- **剪切**：复制 → `deleteNode`（内部自动 `commitToHistory`）→ 移除节点及关联边
- **创建副本**：`JSON.parse(JSON.stringify(source))` 深克隆 → 新 ID → 偏移 `+30,+30` → `addNode`（内部自动 `commitToHistory`）
- **删除**：`deleteNode`（内部自动 `commitToHistory`）→ 移除节点及关联边
- 右键节点时自动选中该节点（`setSelectedNodeIds([node.id])`），与画布右键菜单互斥（click-outside 自动关闭对方）

## 撤销/重做功能完善

### 修复 Ctrl+Y Web 模式失效
- `useKeyboardShortcuts.ts`：将 `Ctrl+Shift+Z` 和 `Ctrl+Y` 拆分为两个独立条件分支
- Ctrl+Z（无 Shift）→ undo，Ctrl+Shift+Z → redo，Ctrl+Y（无 Shift）→ redo
- Tauri 原生快捷键盘也同步注册了 Ctrl+Y

### 将 commitToHistory 收口到 Store Actions
减少遗漏，所有调用 store 变异 action 的路径自动获得历史记录：

| Store Action | 变更 |
|---|---|
| `addNode` | 开头加 `get().commitToHistory()` |
| `addNodeWithEdge` | 开头加 `get().commitToHistory()` |
| `deleteNode` | 开头加 `get().commitToHistory()` |
| `onConnect` | 开头加 `get().commitToHistory()` |
| `pasteNodes` | 开头加 `get().commitToHistory()` |

### 其他补全
- `useKeyboardShortcuts.ts` 键盘 Delete：调用处补 `commitToHistory()` + `e.stopPropagation()`
- `useNodeSnap.ts` `onNodeDragStop`：拖拽结束补 `commitToHistory()`
- `Canvas.tsx` `handleNodesChange` remove 分支：补 `commitToHistory()`
- `useNodeContextMenu.ts`：移除冗余的 `commitToHistory` 调用（store actions 已自动处理）
