## 2026-06-09

- 修复 4 个无效动态导入警告：`MultiSelectToolbar.tsx`(aiService+fileService)、`useNodeCreation.ts`(@tauri-apps/plugin-fs readFile)、`SettingsPanel.tsx`(@tauri-apps/plugin-dialog open) 改为静态导入
- 修复 `@tauri-apps/api/core` 和 `@tauri-apps/api/path` 的无效动态导入：`fileService.ts` 改为静态导入 `convertFileSrc`/`appDataDir`，简化 `getConvertFileSrc()`/`getAppDataDir()`；`SettingsPanel.tsx` 改为静态导入 `invoke`，`isTauri()` 改用 `__TAURI_INTERNALS__` 检测
- 已提交: `e6be113` — 消除构建警告 + TS 编译错误 + MiniMap 样式, 13 files, +54/-58
- 修复 GooeyBtn 悬浮消失：`e6be113` 将 `--x`/`--y` 从纯数字改为带%字符串，但 CSS 中 `calc(var(--x) * 1%)` 自行转单位，`calc("50%" * 1%)` 非法运算 → `::before` 不可见。改回 `String(x)` 传纯数字。已提交: `533dedf`
- 修复选中节点白色半透明矩形边框问题（多次诊断，根因与 backdrop-filter + Chromium 合成层有关，暂以移除 backdrop-filter 为方案）
- 新增「通用模型」功能（设置 → API Key 标签）：
  - 类型：`GeneralModelConfig { id, name, openaiUrl, anthropicUrl, modelId, apiKey, category }`
  - 类型分类：`GeneralModelCategory = 'mixed' | 'text' | 'audio' | 'video' | 'image'`(含颜色/标签映射)
  - Store：`addGeneralModel` / `updateGeneralModel` / `removeGeneralModel`，ID 用 `Date.now().toString(36)+随机码` 生成
  - UI：折叠表单添加新模型；已添加列表显示名称/种类标签/模型ID，可展开编辑或删除
