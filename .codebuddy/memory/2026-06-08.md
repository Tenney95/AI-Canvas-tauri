## 2026-06-08

### GooeyBtn 颜色跟随 Handle 类型色
- GooeyBtn 新增 `hue` prop（number），默认 170，通过 inline style `--hue` 覆盖内置 hue
- TextNode 传入 `hue={234}` 使 GooeyBtn 显示 indigo 色，匹配 `.handle-text` 的 `#6366f1`
- 颜色映射：text=234°（indigo）, image=142°（green）, video=217°（blue）, audio=30°（orange）

### ImageNode / VideoNode / AudioNode 添加 GooeyBtn
- 三个节点的 Handle 中均添加 `GooeyBtn`，颜色跟随各自 Handle 类型
- ImageNode: `hue={142}`（green .handle-image #22c55e）
- VideoNode: `hue={217}`（blue .handle-video #3b82f6）
- AudioNode: `hue={30}`（orange .handle-audio #f97316）

### 新增 360全景图节点（ai-panorama）
- 新建 `src/components/nodes/PanoramaNode.tsx`，支持图片/360预览双模式切换
- 360° 预览器：CSS 3D 立方体 + 鼠标拖拽旋转（yaw/pitch）
- 模式切换按钮（top-right），image ↔ 360
- 支持 Source 角色上传全景图（.png/.jpg/.jpeg/.webp）
- Handle + GooeyBtn（hue=180，青色）
- 修改文件清单：
  - `src/types/index.ts`: NodeType 添加 `'ai-panorama'`
  - `src/components/Canvas.tsx`: nodeTypes 注册 PanoramaNode
  - `src/components/Sidebar.tsx`: generationItems 添加"生成360全景"，移除旧 360-panorama 占位符
  - `src/hooks/useConnectionDropMenu.ts`: ai-text/ai-image 菜单增加"生成360全景图"→ai-panorama；新增 ai-panorama 的出入连线规则
  - `src/components/canvas/ConnectionMenu.tsx`: iconColors 添加 ai-panorama=cyan 色，移除 special 逻辑
  - `src/components/canvas/CanvasContextMenu.tsx`: GEN_NODE_ITEMS 添加，L3_ITEM_COUNT 4→5
  - `src/hooks/useCanvasContextMenu.ts`: addNodeAtCtxPos 添加 panorama 维度处理（300×200）
  - `src/components/nodes/shared/NodeLabel.tsx`: labelConfig 添加 ai-panorama=cyan
  - `src/styles/nodes.css`: 新增 .pano-node / .pano-mode-toggle / .pano-viewer-* / .handle-panorama 样式
- PanoramaNode 上传按钮改为所有角色均可见（不再限制 isSource），移除未使用的 isSource 变量。占位文案更新为"上传全景图或连线生成"

### PanoramaNode 升级为 Three.js WebGL 全景查看器
- 安装 `three` + `@types/three` 依赖
- `PanoramaViewer` 重写为基于 Three.js 的 equirectangular 全景渲染：
  - `THREE.SphereGeometry`（反转法线）+ `MeshBasicMaterial` 实现天空球
  - `PointerEvents` 拖拽旋转（lon/lat），`wheel` 缩放（FOV 20°~110°）
  - `ResizeObserver` 自适应容器尺寸
  - 卸载时完整 dispose 防止内存泄漏
- 顶部工具栏：关闭（退出360）、上传全景图、全屏显示
- 右下角工具按钮：日景/夜景切换（CSS `mix-blend-mode: multiply` 叠加层）
- 左下角提示："拖拽旋转视角，滚轮缩放"
- 全屏模式：`position:fixed;inset:0;z-index:9999` + 半透明遮罩 + Escape 关闭
- CSS 完全重写：移除旧的 CSS 3D 立方体样式，新增 `.panorama-scene-shell` / `.node-floating-toolbar` / `.ftb-btn` / `.panorama-night-overlay` / `.panorama-fullscreen-backdrop` 等
- Node data 新增 `panoEnv`（day/night）和 `panoFullscreen`（boolean）状态
- 修复 PanoramaViewer `releasePointerCapture` 错误：dragRef 新增 `pointerId` 字段，onPointerDown 存储实际 pointerId，onPointerUp 用存储的 pointerId 释放 + try/catch 防护
- 全景预览框重构：预览框充满节点（padding:0; height:100%; align-items:stretch），模式切换移到左上角（right→left），删除日夜切换按钮+叠加层+envMode/toggleEnv 逻辑，toolbar 移到 PanoramaViewer 外侧上方（wrapper + top:-38px）
- 修复全景/图片预览高度丢失（黑屏）+ image-preview-container 不充满节点：根因是父级只有 minHeight 无显式 height，height:100% 无法解析。改为 flex 布局 — .pano-node .node-preview.compact 移除 height:100%；.panorama-viewer-wrapper 和 .panorama-scene-shell 改为 flex:1 + min-height:0；新增 .pano-node .image-preview-container/.image-preview-img { flex:1; min-height:0 }
- 修复宽度不充满节点：.panorama-viewer-wrapper / .panorama-scene-shell / .pano-node .image-preview-container & img 均补上 width:100% + min-width:0
- CSS 拆分：`nodes.css`(1074行→302行) 按类别拆为 nodes.css(通用基础)、nodes-text.css(128行)、nodes-image.css(198行)、nodes-audio.css(61行)、nodes-panorama.css(178行)、nodes-group.css(89行)。Panorama v2 toolbar/ftb-btn 用 `.panorama-viewer-wrapper` 父选择器限定作用域避免污染其他节点。index.css 按 base→category 顺序引入。
- PanoramaNode 新增 ResizeHandle + 上传时自动按图片宽高比计算节点尺寸：导入 `computeImageNodeDimensions` 和 `ResizeHandle`，上传回调改为先计算尺寸再更新 nodeWidth/nodeHeight；compact 模式 `.node` 使用固定 `height` 而非 `minHeight`；非全屏时渲染 ResizeHandle（minWidth=160/minHeight=120）
- 修复全景节点左侧边距 + 360 比例不匹配：`.pano-node .node-preview.compact` 改为 `flex-direction:column; align-items:stretch` 使内容贴边填充；移除 `.panorama-scene-shell` 的 `min-height:160px`；上传时强制 2:1 宽高比（`nodeH = (nodeW - 4) / 2 + 4`），移除 computeImageNodeDimensions 依赖
- 修复全景节点上边距过大：`justify-content: stretch` 是无效 CSS 值（stretch 只能用于 align-items/align-content），浏览器忽略后回退到基础样式的 `justify-content: center`，导致 content 纵向居中留白 → 改为 `justify-content: flex-start`
- 修复 `.pano-mode-toggle` 被 `tooltip.css` 的 `[data-tooltip] { position: relative }` 覆盖导致占高度：新增 `.pano-mode-toggle[data-tooltip] { position: absolute }` 覆盖规则
- PanoramaNode 有内容时隐藏上传按钮：条件从 `!isFullscreen` 改为 `!isFullscreen && !hasImage`，和 ImageNode 行为一致
