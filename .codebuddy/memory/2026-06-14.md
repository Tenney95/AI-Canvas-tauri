# 2026-06-14

## ComfyUI 启动 — 最终方案

### 最终代码
- `spawn_new_console()`: `cmd /k program arg1 arg2` + `current_dir()` + `CREATE_NEW_CONSOLE`
  - `/k` 保活窗口：Python 崩溃也能看到错误
  - 程序路径和参数通过 Rust `Command::arg()` / `args()` 独立传递，Rust 只对含空格路径加引号，不破坏 cmd 解析
- `run_bat_script()`: `cmd /c bat_path` + `current_dir()` — bat 脚本由 ComfyUI 服务进程保持运行

### 踩过的坑
1. `cmd /K "cd /d \"...\" && \"...\""` — Rust args() 自动加外层引号 + 转义 → cmd 收到乱码 → 空命令
2. `cmd /K` + `raw_arg()` 直传 — arg 被整体当单个参数，cmd 不解析
3. `start "" /D "dir" "program" args` — Windows 找不到文件（start 双引号解析问题）
4. 直接 spawn `python.exe` — Python 报错崩溃，控制台立刻消失，看不到错误

### 当前状态
- `spawn_new_console`: `cmd /k "F:\...\python.exe" main.py --skip-manager-update`
  - `arg("/k")` + `raw_arg(&cmd_line)` — 命令拼成单个字符串，raw_arg 原样传递避免 Rust 自动引号破坏
- `run_bat_script`: `cmd /c run.bat`（从 bat 所在目录执行）
- 文件：`src-tauri/src/comfyui.rs`

### 2026-06-14 21:42 — 优先使用秋叶启动器
- 秋叶整合包（`ComfyUI-aki-v1.1`）直接调 `python main.py` 会卡在日志输出阶段
- 原因：秋叶启动器会设置特定环境变量和参数（如 Python `-s` 禁用 user site-packages）
- **优先方案**：检测 `A启动器.exe` / `启动器.exe`，若存在则直接启动（自带完整环境配置）
- **回退方案**：Python 调用增加 `-s` 和 `--listen` 参数
- 新增 `run_exe_new_console()` 函数用于启动器
- 清理了 `PathBuf` 未使用导入和 `main_py` 未读取赋值警告
- 启动优先级：.bat > 秋叶启动器.exe > python main.py
- 移除 `--skip-manager-update` 参数：新版 ComfyUI 的 `main.py` 不再识别该参数，报 `unrecognized arguments`
- 修改位置：`launch_windows()` 第 55 行、`launch_unix()` 第 145 行
- 后续如有需要节点管理器更新跳过，需通过 ComfyUI 配置文件控制

### cmd /k 的坑
- `cmd /k` 语法是 `cmd /k <单个命令字符串>`，把命令拆成多个 args 会导致 cmd 只执行第一个 arg
- 必须用 `raw_arg()` 把完整命令字符串注入，因为 `arg()` 可能加外层引号破坏结构

## 全项目样式统一优化 (22:51)

基于 Impeccable + UI-UX-Pro-Max 技能，对全部 20 个 CSS 文件 + AnimatedButton.tsx 进行了系统性优化：

### 改动范围
- **16 个 CSS 文件** + **1 个 TSX 组件**
- 无新增/删除文件，仅修改现有文件

### 核心变更
1. **Motion Design Tokens** (`base.css`): 
   - `--ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1)`
   - `--ease-out-quart: cubic-bezier(0.25, 1, 0.5, 1)`
   - `--ease-in-out: cubic-bezier(0.65, 0, 0.35, 1)`
   - `--dur-instant: 100ms` / `--dur-fast: 150ms` / `--dur-normal: 200ms` / `--dur-slow: 300ms`
   - `--transition-base` / `--transition-smooth` 复合变量
   - 所有 `transition: X 0.15s ease` → `transition: X var(--transition-base)`

2. **Glass/Frosted Surface Tokens** (`base.css`):
   - `--glass-bg` / `--glass-bg-light` / `--glass-bg-node` — 三级毛玻璃背景
   - `--glass-ring` / `--glass-ring-strong` — 统一玻璃边框
   - `--glass-blur-sm: 6px` / `--glass-blur-md: 16px` / `--glass-blur-lg: 24px` — 三级模糊
   - `.glass-surface` / `.glass-surface--light` 工具类

3. **节点卡片启用毛玻璃** (`nodes.css`): `.node` 现在有 `backdrop-filter: blur(6px)` + `border: 1px solid var(--glass-ring)`

4. **统一玻璃应用到所有面板**: Sidebar、Node Picker、Context Menu、AI Dialog、Workflow Panel、Assets Panel、Settings Cards、Tooltip、Fullscreen Overlays、Slash Commands、Crop Toolbar → 都使用 glass tokens + backdrop-filter

5. **精简间距**:
   - Sidebar 按钮: 38→36px, padding 6→5px, gap 4→2px
   - 面板内边距: 20→16px, 24→18px 等
   - 面板头部: 10→8px/14→12px
   - 空状态: 48→40px padding
   - 多处圆角精简 (16→14px, 8→7px 等)

6. **动画曲线统一**:
   - AnimatedButton: `spring(stiffness:400, damping:17)` → `duration:0.15, ease:[0.16,1,0.3,1]`
   - 所有 CSS @keyframes 动画使用 `var(--ease-out-expo)` / `var(--dur-normal)`
   - 清除所有 `ease` 和 `ease-in-out` → 统一 `ease-out-expo`

7. **Accessibility**: 添加 `prefers-reduced-motion: reduce` 媒体查询

8. **冗余清理**: 消除了 20+ 处硬编码 rgba 值 → CSS 变量; 修复 sidebar.css 与 canvas-context.css 中 `.menu-row` 的重定义冲突

## TSX 内联样式清理 + 子菜单统一 (23:06)

用户指出左边栏子栏目背景色/hover色不统一，且只关注 CSS 不够，TSX 中的内联样式也需要处理。

### canvas-context.css — 消除 .menu-row 全局冲突
- 将 `.menu-row` 及其子类全部 scope 到 `.canvas-ctx-menu` 选择器下
- 避免与 `sidebar.css` 中的 `.menu-row` 冲突（两个文件同名类之前互相覆盖）
- 硬编码值全部转为 CSS 变量：
  - `rgba(255,255,255,0.06)` → `var(--theme-hover)` / `var(--border-subtle)`
  - `#c8c8d4` → `var(--theme-text-secondary)`
  - `#fff` → `var(--theme-text)`
  - `#666680` / `#555566` → `var(--theme-text-muted)`
  - `#9999b0` → `var(--theme-text-secondary)`

### Sidebar.tsx — 消除内联颜色样式
- 当前项目图标 `style={{ color: 'rgba(129,140,248,0.85)' }}` → `className="menu-ico--brand"`
- 项目选中行 `style={{ backgroundColor: 'rgba(99,102,241,0.12)' }}` → `className="menu-row--active"`
- 项目图标/名称选中态 `style={...{ color: 'var(--brand-hover)' }}` → 由 `.menu-row--active` 级联 CSS 控制

### sidebar.css — 新增子菜单统一类
- `.menu-row--active`: 选中项目背景 `var(--brand-alpha-12)`，级联 `.menu-ico` / `.menu-lbl` 颜色 `var(--brand-hover)`
- `.menu-ico--brand`: 固定品牌色图标（当前项目指示）
- `.menu-ico` 颜色 `rgba(232,232,237,0.5)` → `var(--theme-text-muted)`，hover 态 `rgba(232,232,237,0.85)` → `var(--theme-text)`

### base.css — 新增缺失变量
- `--brand-hover: #818cf8`（Sidebar.tsx 中引用但未定义）

### ApiKeySettings.tsx — 消除厂商徽章内联样式
- 4 个厂商徽章 `style={{ background: ..., color: ... }}` → `className="settings-card-badge settings-card-badge--{provider}"`
- ComfyUI 图标 `style={{ color: '#a78bfa' }}` → `className="settings-icon--comfyui"`

### settings.css — 新增厂商徽章颜色变体
- `.settings-card-badge--volcengine` / `--runninghub` / `--grsai` / `--dreamina`
- `.settings-icon--comfyui`: SVG 图标颜色

### SplashScreen.tsx
- 删除冗余 `style={{ background: '#000000', opacity: 1 }}`（class 已有 `bg-black`）

### SettingsPanel.tsx
- `rgba(255,255,255,0.15)` → `var(--white-alpha-15)`
