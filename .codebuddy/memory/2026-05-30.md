# 2026-05-30

## 为所有源文件添加 JSDoc 注释

- 为全部 39 个源文件（组件/服务/Store/Hooks/类型）添加了统一的 JSDoc 文件头注释
- 格式：`/**\n * 组件中文名称 — 功能说明\n */`
- 覆盖 27 个 `.tsx` + 12 个 `.ts` 文件，零 linter 错误

## 更新 README.md 项目结构

- 将 `README.md` 中过时的项目结构更新为当前真实文件树
- 新增 `canvas/`、`nodes/shared/` 子目录，补全所有服务和 Hooks

## App.tsx 毛玻璃效果优化

- 将根容器背景改为 `bg-canvas-bg/80` + `backdrop-blur-2xl`
- 第一次尝试：Rust 端 `window.set_effects(WindowEffectsConfig { effects: [Effect::Acrylic] })` 启用 Windows 原生 Acrylic — 效果覆盖整个窗口，左侧透明区和圆角都被渲染，不够精确
- 最终方案：回退 `lib.rs` 的 `set_effects`，仅用 CSS `backdrop-blur-2xl` 在内部 content div（`rounded-[16px] overflow-hidden`）上，模糊被精确限制在圆角内容区域内
- 注意：CSS `backdrop-blur` 能否在 WebView2 透明窗口下模糊桌面取决于渲染管线实现，若不生效需另寻方案

## 全局 tooltip 统一 + 节点按钮定位修复

- 将全部 7 处 `title=` 原生提示替换为 `data-tooltip` 自定义提示（VideoNode/TextNode/ImageNode/AudioNode/SettingsPanel）
- `tooltip.css`：新增 `.node-upload-btn[data-tooltip]` 规则 — tooltip 向下弹出（避开节点 `overflow: hidden`）
- 修复 `[data-tooltip] { position: relative }` 覆盖 `.node-upload-btn { position: absolute }` 的特异性冲突：新增 `.node-upload-btn[data-tooltip] { position: absolute }` 以 0,2,0 特异性覆盖 0,1,0
- `sidebar.css`：shadow 改为 `8px 0 32px` 向右投影
- `App.tsx`：背景透明度从 `/80` → `/95` → 全不透明 → `/98` → `bg-canvas-bg/[0.98]`（Tailwind 不支持 `/98`）

## FreeAnglePanel — 控制角度面板组件

- 新建 `src/styles/freeangle.css`：面板所有样式（overlay、3D 正方体 CSS transforms、滑块、模型选择器、按钮）
- 新建 `src/components/nodes/shared/FreeAnglePanel.tsx`：完整的自由角度控制面板
  - 3D 正方体预览（CSS transform: rotateX/rotateY），支持 PointerEvent 拖拽旋转
  - 图片映射到正方体正面（使用节点 imageUrl）
  - 三个滑块：水平角度(0-360°)、垂直角度(-30~60°)、距离(0.1-2)
  - 模型选择器：4 个供应商分组下拉（GRSAI / APIMart / RunningHUB模型 / RunningHUB工作流），默认"控制摄像机"
  - 重置/调试/生成按钮，点击外部关闭、ESC 关闭
  - 通过 createPortal 渲染到 document.body
- 修改 `ImageNodeToolbar.tsx`：新增 `onMultiAngle` prop，"控制角度"按钮从 noop 改为有效回调
- 修改 `ImageNode.tsx`：导入 FreeAnglePanel，添加 isFreeAngle 本地状态和 open/close/generate 回调

## 修复：点击"控制角度"按钮没反应

- **根因**：React Flow v12 在 `pointerdown` 阶段捕获节点拖拽事件。`ImageNodeToolbar` 只在 `onClick` 上做了 `stopPropagation`，但 `pointerdown` 事件先于 `click` 被 React Flow 截获，导致节点拖拽启动后 `click` 被取消
- **修复**：`ImageNodeToolbar.tsx` 新增 `blockPointer` handler（`stopPropagation` + `preventDefault`），工具栏容器 div 和按钮上加 `onPointerDown={blockPointer}`，防止 pointerdown 冒泡到 React Flow
- **二次修复**：`pointerdown` 上的 `preventDefault()` 在浏览器层面阻止了后续 `click` 事件的触发（根据 DOM 规范，pointerdown 的 preventDefault 会 suppress click），导致按钮 onClick 永远不执行 → 移除 `preventDefault()`，仅保留 `stopPropagation()`
- **三次修复（最终）**：React Flow v12 使用原生 DOM 事件监听器处理拖拽，React 合成事件的 `stopPropagation()` 无法阻止原生监听器。正确做法是使用 React Flow 内置的 `nodrag` 机制：给工具栏容器及所有子级容器添加 `nodrag` class → React Flow 内部识别该 class 后自动跳过拖拽、不消费事件 → click 正常触发。同时移除了之前添加的所有 `onPointerDown` 手动拦截代码
- **修复 import 路径**：`FreeAnglePanel.tsx` 中 `freeangle.css` 的引用路径少了一层 `../`（文件在 `src/components/nodes/shared/`，深三层），改为 `../../../styles/freeangle.css`
- **修复 3D 正方体拖拽反转 360°**：`handleCubePointerMove` 中 `normalizeAngle(startRot - dx * 0.5)` 会在跨 0/360 边界时跳变（如 359.5→4.5），CSS `rotateY` 看到 ~355° 瞬间变化造成视觉反转。修复：拖拽期间不对 rotation 归一化，让 rotation 自由累积（CSS 天然支持任意角度），仅在显示值和生成参数输出时做 `normalizeAngle`
- **修复垂直拖拽方向反转**：`handleCubePointerMove` 中 `dy` 系数从 `+` 改为 `-`，向下拖 → pitch 减小（向下俯），与直觉一致
- **实现 APIMart 角度控制 API**：新建 `src/services/apimartService.ts`，封装完整流程：① data URL 自动上传到 APIMart (`POST /v1/uploads/images`) → ② 提交图像生成任务 (`POST /v1/images/generations`, 带 rotation/pitch 映射的 camera prompt) → ③ 轮询 (`GET /v1/tasks/{task_id}`) → ④ 返回图片 URL 列表。`ImageNode.tsx` 的 `handleFreeAngleGenerate` 已对接：调用服务 → 下载结果图片转 data URL → 计算节点尺寸 → `store.addNode()` 创建新图片节点
- **FreeAnglePanel 模型下拉支持 Key 缺失禁用**：`FreeAnglePanel.tsx` 导入 `useAppStore` 读取 `config.providers`，新增 `isModelAvailable()` 判断 provider 是否有 API Key（`runninghubwf` 共享 `runninghub` 的 key）。无 Key 的模型项添加 `disabled` class（opacity 0.38、cursor not-allowed）、🔒 锁图标、hover tooltip 提示"请先在设置中配置 XX API Key"，点击时 `handleSelectModel` 拒绝选中并弹 toast
- **修复 APIMart SubmitResponse 解析**：实际返回 `{ code: 200, data: [{ task_id }] }`，原代码按 `{ task_id }` 平级取值导致 `undefined` → 改为 `result.data?.[0]?.task_id`
- **生成节点位置改为原节点右侧**：`ImageNode.tsx` 新节点 position 从 `{x: currentPos.x + i*40, y: currentPos.y + 180}` 改为 `{x: currentPos.x + nodeWidth + 40 + i*40, y: currentPos.y + i*40}`，多张结果向右排列、轻微向下错开
- **FreeAnglePanel Key 检查提升到 group 级别**：`isModelAvailable` → `isGroupAvailable`，判断从 per-model 改为 per-group（groupId 映射到 providerKey）。不可用分组：header 加锁图标替代展开箭头、opacity 0.38 + cursor not-allowed、hover tooltip、不可展开；子菜单所有 item 自动 disabled，无需逐项判断。移除 `handleSelectModel` 中的 toast 逻辑和不再需要的 `showToast` 绑定。
- **FreeAnglePanel group 图标统一为文字简称**：4 个 group 全部改为 `iconType: 'badge'` + `badgeText`（GAI / AM / RH / RH），移除图片图标。模板简化：group header 始终渲染 `fa-model-icon-text`，不再分支 img
- **ModelSelector 加 API Key 检查 + FreeAnglePanel 复用 ModelSelector**：① `ModelSelector.tsx` 新增 `isGroupAvailable()`（同 FreeAnglePanel 模式：`runninghubwf` → `runninghub` key）、`defaultExpandedGroupIds` prop（其余默认折叠）、不可用分组显示 lock 图标 + opacity 0.38 + tooltip + 不可展开、子项统一 disabled。② `FreeAnglePanel.tsx` 删除自定义下拉（约80行 JSX + CSS），改用 `<ModelSelector nodeType="ai-image" groups={ANGLE_MODEL_GROUPS} defaultExpandedGroupIds={['runninghubwf']} />`，数据转为 `ModelGroup[]` / `ModelOption[]` 格式。③ `model-selector.css` 新增 `.model-group-header.disabled` / `.model-item.disabled` / `.model-lock-icon` 样式。④ `freeangle.css` 删除约 180 行已废弃的 `fa-model-*` 样式（menu/dropdown/group header/submenu/menu item/lock/scrollbar）。⑤ `FreeAnglePanel.tsx` 移除不再需要的 `ModelGroupOption`/`ModelItemOption` 类型、`isGroupAvailable`/`toggleGroup`/`menuRef`/`triggerRef`/`menuOpen`/`expandedGroups` 等状态和逻辑。
