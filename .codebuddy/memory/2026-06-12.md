- **移除「OpenAI兼容的API格式」卡片**：
  - 根因：该卡片是早期设计，已被「通用模型」完全取代（`config.providers.openai` 在 `aiService.ts` 中零引用）
  - 改动：
    1. `ApiKeySettings.tsx`：删除 OpenAI 卡片 (~47行) + 移除未使用的 `setProviderUrl` 解构
    2. `testConnection.ts`：删除 `testOpenAICompatible` 函数 + 从 `ProviderTestKey` 移除 `'openai'`
    3. `aiService.ts`：从 `DEFAULT_BASE_URLS` 移除 `openai` 条目
  - 其他文件中的 `openai` 引用（模型描述文字 / `GeneralModelConfig.openaiUrl` 字段名）属于「通用模型」功能，不受影响
- **修复新建节点默认模型报错「请先在底部模型选择器中选择一个模型」**：
  - 根因：所有新建 generator 节点的创建路径（`useNodeCreation` 四个 add 函数、`useCanvasContextMenu.addNodeAtCtxPos`）都没有给 `data.model` / `data.provider` 赋值，导致 `AINodeDialog.onSubmit` 校验失败
  - 修复：
    1. `useNodeCreation.ts` 新增 `loadDefaultModel(nodeType)` 工具函数，从 `localStorage` 的 `canvas-model-prefs` 键读取该节点类型上一次选择的模型，解析出 `model`（全值如 `'apimart/gpt-5.4'`）和 `provider`（如 `'apimart'`）
    2. `addTextNode` / `addImageNode`（generator 路径）/ `addVideoNode`（generator 路径）/ `addAudioNode`（generator 路径）：创建节点 data 时自动填充 `model` 和 `provider`
    3. `useCanvasContextMenu.ts`：同样新增 `loadDefaultModel` + `addNodeAtCtxPos` 中 generator 节点自动填充
  - 首次使用（无 localStorage 记录）时仍会提示选择模型，这是预期行为
- **项目切换后自动 Fit View**：
  - 根因：`ReactFlow.fitView` prop 只在组件首次挂载时执行一次，项目切换后仅更新 nodes/edges 数据不触发视口自适应。且已有的 F 键 `canvas-fit-view` 自定义事件无人监听（死功能）。
  - 修复（2 文件）：
    1. `Canvas.tsx`：在 `CanvasInner` 新增 `useEffect` 监听 `canvas-fit-view` 事件 → `requestAnimationFrame` 后调用 `reactFlowInstance.fitView({ padding: 0.2 })`
    2. `store.projects.ts`：4 处 `set()` 后通过 `setTimeout(..., 0)` 派发 `canvas-fit-view`：
       - `createProject`（新建空画布）
       - `switchProject`（切换有数据/空项目两条分支）
       - `deleteProject`（替换默认画布 / 加载下个项目数据两条分支）
  - 副作用：F 键 fit-view 快捷键现在起效了（之前事件派发但无人监听）
- **修复侧边栏/连线拖放创建节点默认模型缺失**：
  - 根因：之前只修了 `useNodeCreation`（双击/文件拖放）和 `useCanvasContextMenu`（右键），遗漏了两个路径：
    1. `Sidebar.tsx` → `NodePicker.handleAddNode`：侧边栏 + 按钮 hover → NodePicker 点击创建，手动构建 `nodeData`，无 model/provider
    2. `useConnectionDropMenu.ts` → `handleSelect`：拖拽节点 Handle 连线 → 连接菜单选择类型创建，手动构建 `nodeData`，无 model/provider
  - 修复：两处各添加 `loadDefaultModel` 逻辑，从 `localStorage('canvas-model-prefs')` 读取上一次选中的模型，自动注入 `data.model` / `data.provider`
  - 至此所有 generator 节点创建路径全覆盖：双击画布 ✓ / 右键创建 ✓ / 侧边栏点击 ✓ / 连线拖放 ✓
- **AI 输出历史记录功能**：
  - 用户需求：统一记录所有 AI 生成的输出，不限条数，用户手动管理，底部抽屉样式
  - 改动（8 文件）：
    1. `src/types/index.ts`：新增 `OutputHistoryEntry` 接口 + `BaseNodeData.outputHistory`
    2. `src/store/store.historyRecord.ts`：**新建** History Record Slice（recordOutputHistory / deleteHistoryEntry / clearNodeHistory / clearAllHistory）
    3. `src/store/store.ui.ts`：新增 `historyPanelOpen` 状态 + setter
    4. `src/store/useAppStore.ts`：组合新 Slice
    5. `src/components/OutputHistoryPanel.tsx`：**新建** 底部抽屉面板（AnimatePresence + y 轴滑入动画），支持类型筛选、搜索、时间线展示、查看节点/复制/删除/清空/导出 JSON
    6. `src/components/Sidebar.tsx`：新增「输出历史」按钮（时钟图标），位于工作流按钮下方
    7. `src/components/nodes/AINodeDialog.tsx`：onSubmit 各分支 + onPassThrough 成功后调用 `recordOutputHistory`，失败也记录
    8. `src/components/canvas/MultiSelectToolbar.tsx`：批量生成成功后同样记录
    9. `src/App.tsx`：挂载 `<OutputHistoryPanel />`
    10. `src/components/Canvas.tsx`：新增 `canvas-focus-node` 事件监听，历史面板「查看节点」时平滑定位
  - 面板特性：底部抬起抽屉（max-h-[75vh] max-w-[720px]），拖动条指示器，类型 Tab 筛选，实时搜索，相对时间，可展开提示词，文本输出预览/图片缩略图，节点已删除灰色标记
  - **调整布局**：将搜索框合并到分类选项卡所在行（filter tabs + search 同在一行 flex），分类在左、搜索框在右 flex-1 撑满
- **输出历史面板 UI 优化**：
  - 移除装饰性拖动条手柄、合并搜索框与分类筛选到同一行
  - 历史记录同时展示线上地址和本地地址，地址单行显示超出隐藏+复制按钮
  - **修复线上地址记录错误**：`AINodeDialog.tsx` / `MultiSelectToolbar.tsx` 共 6 处 `recordOutputHistory` 调用的 `mediaUrl` 从 `saved?.assetUrl` 改为 `result.url`（旧代码记录了 Tauri asset 本地地址而非 API 原始链接）
- **修复 TS 构建错误**：`OutputHistoryPanel.tsx` 移除未使用的 `NodeType` import；`store.historyRecord.ts` 未使用的 `get` 改为 `_get`
- **FreeAnglePanel 改为通用弹窗**：复用 `FullscreenOverlay` 作为容器壳，移除自定义 overlay/panel/header/portal/ESC 逻辑（~40行）；清理 `freeangle.css` 和 `nodes.css` 中已废弃的样式；`FullscreenOverlay` 统一提供 AnimatePresence 动画 + backdrop + ESC 关闭
- **修复 Tauri overlay CSS 选择器不生效**（2 处）：
  1. `nodes.css`：选择器 `[data-tauri]` → `[data-tauri-window]`（与 `App.tsx` 实际设置的属性名一致）
  2. `App.tsx`：`FullscreenOverlay` 通过 `createPortal` 渲染到 `document.body`，不是 `data-tauri-window` div 的后代，后代选择器匹配不到 → 新增 `useEffect` 给 `document.body` 也设置 `data-tauri-window=""`，让 CSS `[data-tauri-window] .fullscreen-overlay` 能选中 Portal 弹窗
- 已提交: `0ff4f68` — 输出历史面板 + 默认模型修复 + Fit View + 清理 OpenAI 卡片, 22 files, +899/-25
- **图像功能整合 + 裁切编辑器**：
  - 新建 `src/components/nodes/shared/image/` 子文件夹，将图像专属文件移入：`ImageNodeToolbar.tsx` `FreeAnglePanel.tsx` `MattingEditor.tsx` `MattingToolbar.tsx` `imageUtils.ts`
  - 新建 `CropEditor.tsx`：全屏裁切编辑器（Canvas 渲染蒙版 + 9 宫格线 + 8 个手柄），支持自由/1:1/4:3/16:9/3:4/9:16 六种宽高比，拖拽移动/缩放裁切框，确认后创建新图像节点
  - 新建 `crop.css`：裁切编辑器样式，已在 `index.css` 引入
  - `ImageNode.tsx`：更新导入路径，接入 `onCrop` → 打开 CropEditor，保存时自动 persist 到项目 data + 创建新节点
  - `ImageNodeToolbar.tsx`：裁切按钮从 `noop` 改为 `onCrop` 回调
  - 尚待实现的功能按钮：扩图、宫格裁切、重绘、高清、自动识别主体、擦除、标注
- **修复裁切弹窗只显示 header 不显示内容**：
  - 根因：`fullscreen-panel` 只有 `max-height: 92vh` 无明确高度 → `fullscreen-body` 的 `flex: 1` 坍缩为 0
  - `crop.css`：`.crop-body` 添加 `min-height: 80vh`（参考 `fullscreen-body--pano`）
  - `.crop-image`：去掉 `position: absolute`，让 flex 容器自然居中图片
  - `.crop-canvas`：改为 `top: 50%; left: 50%; transform: translate(-50%, -50%)` 与图片对齐
  - `CropEditor.tsx`：`layoutRef` 移除 `imgX/imgY`，`handleConfirm` 裁切坐标映射去掉偏移量（Canvas 与图片已居中对齐）
- **CropEditor 改用 react-image-crop 库**：
  - 安装 `react-image-crop`（6M+/周，5KB gzipped，TypeScript 原生支持）
  - `CropEditor.tsx` 重写：从 ~300 行自定义 Canvas+PointerEvent → ~180 行 react-image-crop，删除 clamp/constrainAspect/hitTest/clientToLocal/doResize/dragState 等手写交互逻辑
  - `ReactCrop` 自动提供：蒙版/裁切框/手柄/九宫格线/拖拽/缩放/触屏/宽高比锁定
  - `crop.css`：移除 `.crop-canvas` Canvas 样式，新增暗色主题 ReactCrop CSS 覆盖（`.ReactCrop`/`.ReactCrop__crop-selection`/`.ReactCrop__drag-handle`/`.ReactCrop__line`）
  - `handleConfirm` 使用 `completedCrop: PixelCrop` 直接坐标，不再需要手动 scale 计算
- **修复远程图片裁切 CORS 报错**：
  - 问题：`https://ts.apib.ai` 等外部服务器不返回 `Access-Control-Allow-Origin`，`crossOrigin="anonymous"` 导致图片加载失败，去掉则 canvas 被污染
  - Rust 端：添加 `reqwest` + `base64` 依赖，新增 `fetch_image_data_url` 命令，用原生 HTTP 下载图片返回 base64 data URL（绕过 WebView CORS）
  - `fileService.ts`：新增 `fetchImageForCrop()` 辅助函数，判断远程 URL 时调用 `invoke('fetch_image_data_url')`
  - `CropEditor.tsx`：去掉 `<img crossOrigin="anonymous">`，`handleConfirm` 改为 async，通过 `fetchImageForCrop` 获取安全 URL，再 `new Image().decode()` 绘制到 canvas
- **提升编辑器垂直空间**：
  - `.prompt-editor`：`max-height` 从 `200px` → `min(50vh, 600px)`，加 `overflow-wrap: break-word`
  - `.ai-dialog-float .prompt-editor`：`max-height` 从 `140px` → `min(50vh, 400px)`
  - `.ai-dialog-float`：加 `max-height: calc(100vh - 32px)` + `overflow-x: hidden` 防水平溢出
  - `.prompt-input-wrap`：加 `min-width: 0` 防止 flex 子元素撑破容器
  - `SettingsPanel.tsx` 内容区：加 `overflow-x-hidden`
- **图片编辑器充满容器 + 滚轮缩放**：
  - `.crop-body`：移除 `min-height: 80vh`
  - `.crop-stage`：`overflow: hidden` → `overflow: auto`（缩放后支持滚动查看）
  - `.crop-image`：`max-height: calc(92vh - 48px)`（vh 直接约束，绕过 ReactCrop inline-block 链）
  - `CropEditor.tsx`：新增 `scale` state + `handleWheel` 滚轮缩放（0.1~5 倍，±0.05/格）+ `handleDoubleClick` 双击重置 100%
  - 新增 `.crop-zoom-stage` flex 容器承载 `transform: scale()`，`transform-origin: center`
  - 新增 `.crop-zoom-indicator` 右下角缩放百分比（非 100% 时显示）
  - 关闭编辑器时重置 `scale=1`
- **修复滚轮 preventDefault 被动事件警告**：React 的 `onWheel` 在浏览器中被当作 passive 事件，`e.preventDefault()` 无效。改用 `useEffect` + `stageRef.current.addEventListener('wheel', fn, { passive: false })` 手动绑定非被动监听，`onWheel` 属性从 JSX 中移除。
- **修复裁切结果与框选位置不一致**：`completedCrop` 的像素坐标基于图片 DOM 渲染尺寸（`clientWidth/Height`），需乘以 `naturalWidth/clientWidth` 换算到自然分辨率后再 `drawImage`，否则裁切偏位。
- **确认裁切后自动关闭弹窗**：`handleConfirm` 末尾调用 `onClose()` 并重置所有状态（crop/completedCrop/aspect/scale）。
- **裁切改为后台异步 + 即时关闭 + loading 节点**：
  - `CropEditor`：新增 `onStart` prop，点击确认后立即调用 `onStart?.()` → 关闭弹窗 → 后台异步裁切 → 回调 `onSave`
  - `ImageNode`：新增 `pendingCropNodeId` ref + `handleCropStart`（创建 `status: 'loading'` 的新节点） + `handleCropSave` 改为更新预创建节点
  - 失败处理：裁切失败时删除 loading 节点并 toast 提示
- **修复二次裁切失败（canvas 跨域污染，共 3 轮迭代）**：
  - 第 1 轮：发现 `convertFileSrc()` 返回 `https://asset.localhost/...` 而非 `asset://`，添加 `includes('asset.localhost')` 检测
  - 第 2 轮：用 `fetch()` → `blob` → `URL.createObjectURL(blob)` 尝试绕过 → 仍污染（Tauri WebView 中跨源 fetch 的 blob 创建的 blob: URL 可能继承跨源标记）
  - 第 3 轮（最终修复）：用 `fetch()` → `blob` → `FileReader.readAsDataURL(blob)` → `data:` URL，`data:` URL 绝对同源不污染 canvas
- **修复裁切内容与框选不一致（缩放时坐标换算错误）**：`ReactCrop` 的 `onComplete` 返回的 `PixelCrop` 坐标基于视觉渲染尺寸（`getBoundingClientRect()` 受 CSS `transform: scale()` 影响），但之前 `scaleX = naturalWidth / clientWidth` 用的 `clientWidth` 不受 transform 影响。缩放后 `completedCrop` 是缩放后的像素值，除以 `clientWidth` 得到的比率就错了。修复：`visualW = clientWidth * scale`，`visualH = clientHeight * scale`，再用 `naturalWidth / visualW` 计算换算比例。
- **修复 AI 生成图片后保存报 CORS 错误**：`downloadUrlAndSave` 用浏览器 `fetch()` 直接下载远程 URL（如 `https://ts.apib.ai/...`），被 CORS 策略拦截。改用 `invoke('fetch_image_data_url', { url })` 走 Rust reqwest 原生 HTTP 下载（绕过 WebView CORS），再将返回的 base64 data URL 解析为二进制写入文件。此修复同时覆盖 `AINodeDialog.tsx`（图像/视频/音频）和 `MultiSelectToolbar.tsx` 中的调用。
