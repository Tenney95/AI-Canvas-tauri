# 2026-06-18

## ImageNodeToolbar 重绘按钮激活
- `ImageNodeToolbar.tsx`：新增 `onRepaint` prop，重绘按钮从 `onClick={noop}` 改为 `onClick={handleAction(onRepaint)}`
- `ImageNode.tsx`：新增 `handleRepaint` 回调，直接调用 `useAppStore.getState().openNodeDialog()` 绕过 Canvas.tsx 对已有 imageUrl 节点的拦截
- 重绘时传入节点位置，PromptPanel 弹窗正确出现在节点下方
