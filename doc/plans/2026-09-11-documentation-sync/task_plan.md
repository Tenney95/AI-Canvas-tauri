# 近期功能文档同步

## 目标与范围

依据 `eb006b8` 之后的提交及当前源码，同步操作说明书、四种语言的项目 README、站点 README 和受影响的当前模块文档。版本以 `package.json` 为准；区分已发布基线与后续源码更新。

仅更新文档，不改应用实现、依赖、第三方 README 或冻结历史。经用户确认后进行本地提交，不推送或发布。保留已有 `.gitignore` 改动。根目录旧计划属于其他任务，使用本目录独立记录。

## 阶段

1. [complete] 核对近期功能、现有文档及真实操作入口。
2. [complete] 更新说明书、五份 README 与对应模块文档。
3. [complete] 校验编码、链接、说明书脚本和图片完整性，复核范围。

## 验收与回滚

- 用户操作与当前 UI/源码一致，多语言核心能力保持一致。
- 模块说明记录职责与边界，既有专项验收保留原日期和限制；不将旧测试说成本次执行。
- 文档路径与锚点有效，UTF-8 无乱码，说明书内嵌图片数据不变。
- 回滚仅撤销本次文档差异；不触碰并行工作。

## 执行限制

- 系统 `python` 命令不可用，技能会话恢复脚本未运行；已直接读取旧计划并核对 Git 状态恢复上下文，不安装依赖。

- 文档编辑脚本第一次因模板字符串反引号转义错误在解析阶段退出，未写入文件；改用占位替换后完成该批更新。

## 完成与验证

- 更新操作说明书、四种语言项目 README、站点 README 和 11 份当前 doc 文档；本目录三份记录共计 20 份本任务文档。
- 20 份文档严格 UTF-8 检查、406 个本地 Markdown 链接及锚点检查通过；说明书 92 个唯一 ID、内部跳转和复制目标有效。
- 说明书内联脚本 ESLint、CSS 解析通过；8 张内嵌截图及两份工作流 API JSON 示例与更新前一致，示例 JSON 可解析。四种 README 版本一致，许可、联系方式和贡献者部分不变。
- 本地服务返回 200；浏览器默认 1280×720 视口下新增资产操作段落排版正常，无横向溢出。MCP 目录搜索匹配正常，清空后恢复完整目录。
- 本轮验证属于文档与页面检查，没有重新执行应用功能回归、实际模型生成或平台验收。保留已有专项记录的历史验收限制。
- 工作期间另有 MCP 代码、测试和专项计划的并行改动，未纳入本次编辑；本次按用户确认的文档清单进行本地提交，不包含 `.gitignore` 或其他任务改动。
