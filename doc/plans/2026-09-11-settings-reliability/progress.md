# 工作记录

## 2026-09-11

- 用户授权优化本次排查出的所有设置可靠性问题，并询问更好的存储/读取方案。
- 检查工作区，仅 .gitignore 有既有修改，本任务不修改它。
- 已创建独立专项计划、依据和进度 3 个 UTF-8 文档，用户确认后实施第一阶段。
- 已完成官方方案比较及逐阶段文件清单：保留现有介质，先补失败保护，再处理局部保存/状态/退出，最后加固原生锁与持久性。
- 已核验拟新增源码/测试同名文件不存在；仓库现有 fs2 依赖可复用。
- 第一阶段已完成：8 个源码文件与 6 个测试文件均在确认清单内，未新增依赖、修改 Rust、安全配置或 README。既有 .gitignore 保持不动；未提交、打包或发布。
- MCP 区分缺失/失败，并发初始化复用结果；读失败不覆盖，失败轮换保留活动令牌，会话降级提示可见。工具栏局部事务、加载保护、保存失败草稿保留和过期异步结果保护已落地。
- 定向回归 14 文件、184 项通过：storageDiagnostics、mcpSessionConfig、providerSecretService、toolbarPersistence、useToolbarEdit、mcpControlSettings、configSecretStorage、indexedDbService、config、projects、useMainWindowSize、toolbarRegistry、nodeToolbarShell、nodePluginToolbarPerformance。Vitest 排除了嵌套的 .planning 副本。
- npm run typecheck、npm run test:typecheck、14 个改动源码/测试文件的 ESLint（零警告）通过；git diff --check 通过。新提示复用 ui-alert--warning，已核对明暗主题变量；未进行截图验收或真实桌面重启/原生文件故障验收。
- 用户已确认第二阶段，并明确将独立素材窗口与画布圆环菜单一并修复。
- 第二阶段完成普通字段补丁及事务内冲突检测、Store 修改序号/保存状态/基线、凭据显式变更意图、读取部分凭据失败时的普通配置可用性，以及主窗口关闭时的防抖任务/工具栏草稿收集与队列等待。
- 旧模型转换在迁移 Key 前执行，成功后同步清理旧明文和更新字段；失败保留原记录并阻止保存。编辑器重建渠道对象仍保留凭据引用和版本；迟到保存/重新加载不能把已换版本的缓存 Key 重新当成当前值。
- 保存失败不会继续显示完成或关闭圆环编辑；独立素材窗口保留失败的目录选择，重试重新读取最新配置，只提交目录字段。设置页提供错误、重试和重新加载入口。
- 第二阶段回归包含第一阶段相关范围，共 20 个测试文件、244 项通过，排除了 .planning 副本。应用类型、测试类型、33 个改动源码/测试文件的定向 ESLint（零警告）、严格 UTF-8/乱码检查和 git diff --check 通过。
- 并行任务的 Agent/视频编辑器代码及专项计划、已有 .gitignore 不属于本次修改范围；未覆盖、提交或回滚这些改动。本阶段未新增依赖、修改 Rust/安全配置、启动用户应用、打包或发布。
- UI 新提示复用已有主题 token 和 ui-alert/ui-btn，未做浏览器/桌面主题截图验收。单元测试使用隔离存储及故障注入，不代表真实退出重开、多进程或磁盘故障验收。
- 用户已确认第三阶段，现已完成：原生凭据使用有界跨进程锁、预期旧值/指纹校验、同步落盘和脱敏错误码；固定令牌创建竞争读取胜出值，轮换冲突保留活动令牌。
- 删除渠道先提交普通配置，再按原凭据指纹清理；配置提交失败保留 Key，清理失败持久化不含明文的待办，加载/显式重试可收敛。已提交部分推进 Store 基线，后续重新创建渠道仍能正确保存；原生值已变化则作废旧清理，保留新值。
- 仅配置和工具栏低频事务采用 strict durability；不支持参数的旧运行时可兼容回退，权限和容量错误不回退。工具栏同类型基线冲突保留草稿，重试不偷换基线，用户确认后重新加载。
- 最终前端回归 21 文件、307 项通过，覆盖服务、Store、设置与工具栏交互、渠道配置调用方及原生命令 ACL。npm run typecheck、npm run test:typecheck 与 34 个改动源码/测试文件的定向 ESLint（零警告）通过；最后一次 Store 调整后已再次执行相关 ESLint、类型与合并回归。
- 本任务 41 个源码、测试与文档通过严格 UTF-8 解码和替换字符检查，git diff --check 通过；最终状态检查未发现本任务范围外的意外修改，既有 .gitignore 保留。
- Rust 凭据测试 10 项、路径边界测试 7 项通过（--no-default-features），包含真实子进程竞争写入、持锁进程退出释放、超时、损坏文件及暂存失败保护。默认特性 cargo check --lib 通过；默认特性 cargo test 因现有 ONNX 二进制与 MSVC 工具链缺失 C++ 符号而链接失败，本任务未改依赖或工具链。
- 三个模块文档与专项计划已同步。原生凭据 JSON 格式、IndexedDB schema 和权限边界保持兼容；本阶段未新增依赖、读取真实凭据、启动用户应用、提交、打包或发布。真实桌面退出重开、主题截图、Unix 运行及断电故障仍未验收。
