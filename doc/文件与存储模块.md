# 文件与存储

负责项目与配置持久化、本地媒体引用、项目导入导出、资产索引、回收站和存储健康。凭据的原生保护边界见[桌面运行时与安全](./桌面运行时与安全模块.md)。

## 主要入口

| 入口 | 职责 |
|---|---|
| [fileService.ts](../src/services/fileService.ts) / [fs](../src/services/fs/) | 统一前端文件能力与资产管理 |
| [mediaUrl.ts](../src/utils/mediaUrl.ts) | 媒体 URL 来源分类与本地 asset/file 路径解码 |
| [indexedDbService.ts](../src/services/indexedDbService.ts) / [schema.ts](../src/services/indexedDb/schema.ts) | 数据库 CRUD 与 schema 版本权威 |
| [storageService.ts](../src/services/storageService.ts) / [configPersistenceQueue.ts](../src/services/configPersistenceQueue.ts) / [providerSecretService.ts](../src/services/providerSecretService.ts) | 配置保存顺序、旧凭据迁移与原生凭据桥接 |
| [projectTransferService.ts](../src/services/projectTransferService.ts) | 项目整体导入导出 |
| [storageHealth.ts](../src/services/fs/storageHealth.ts) / [原生 files](../src-tauri/src/files/) | 存储检查与原生传输/归档能力 |

## 关键边界

- 画布无修饰键 `Tab` 打开/收起左侧资产库，`Esc` 或收起按钮关闭；设置的快捷键页列出“资产库 — Tab”，名称复用现有多语言词条。快捷面板复用 `AssetsPanel.tsx` 的项目文件、全局资产、创作资产、搜索筛选和拖拽；每次从画布打开默认跟随当前项目。原资产库入口保留大弹窗，左侧文件固定三列，范围与搜索同排，不改写大弹窗列数。
- 第四个页签“节点列表”浏览当前画布的全部节点类型（包括笔记和插件节点），计数、名称和列表随画布更新，不受项目文件页签的跨项目范围影响。支持名称、类型及编号搜索与增量加载；只在节点页订阅节点标识和内容，移动位置不会触发列表内容重绘。“查看节点”复用历史记录的 `canvas-focus-node` 事件及回弹效果；抽屉保持打开，大弹窗关闭后定位，迟到请求在项目变化或节点已删除时不再发送。
- 节点列表使用 [CanvasNodeCardContent.tsx](../src/components/assets/CanvasNodeCardContent.tsx) 展示大卡片：图片复用画布缩略图缓存，视频展示已有封面或共享封面缓存，文本显示摘要，音频按需播放。输入/输出端口叠放在预览内，不增加底部行高；音频端口避让播放器。媒体按视口加载，旧请求与预览租约在切换、离屏和卸载后释放；小图无需缩略图时沿用画布的原图回退行为。
- 左侧节点卡片的输入/输出端口可拖到画布节点的对应端口或主体左右半区；[assetNodeConnection.ts](../src/utils/assetNodeConnection.ts) 复用实际端口及 Store `onConnect`，创建正常画布边，进入原有项目保存、撤销与重做流程。拖动预览不写 Store；松开时重新校验项目、节点与启用端口，拒绝重复、自连接和同方向连接。Esc、失焦、指针取消、面板卸载或源端口失效时取消；空白处不新增节点。画布笔记不展示虚构端口，插件使用启用清单的真实端口 ID 并要求精确命中目标端口；大弹窗仅展示内容和查看按钮。
- 资产快捷键不抢占输入框、可编辑内容、交互控件和其他弹窗的 Tab 焦点导航。左侧面板避让侧栏且不遮挡其余画布；异步文件列表在关闭、切换项目或发起新读取后忽略旧结果。
- 数据库升级必须兼容已有项目；schema 版本以源码为准，不在模块说明中固定副本。
- 数据库打开失败必须释放失败缓存，允许后续读取重新连接；其他实例请求升级时主动关闭旧连接。升级被占用时明确拒绝本次读取，并释放随后才打开的连接。旧版本遇到更高 schema 必须拒绝，不删除数据库或绕过版本校验。
- 配置读取只有记录确实不存在才返回空值；数据库读取或凭据迁移失败必须向上抛错。配置加载及重新加载期间禁止保存默认值，失败时保留内存配置并提示，同时中止后续项目初始化，避免使用未确认的数据目录；凭据迁移不完整时保留原数据库记录。仅需主题的独立窗口使用不读取凭据的入口，读取失败时保留当前主题。
- 配置保存入队前捕获快照，在同一队列中读取已保存配置、核对/写入凭据并等待数据库事务完成或中止；旧凭据迁移和凭据删除共用队列，单次失败不阻断下一次操作。支持 Web Locks 时使用同源排他锁，锁失败直接拒绝；缺少该 API 时仅保证当前窗口顺序。旧客户端不遵守锁协议、不同窗口后来提交陈旧整份配置的字段覆盖，不由该队列解决。
- 未变更的 API Key 仍核对原生值，但不重复写入；读取未知或更新失败时不删除既有引用，并拒绝覆盖原数据库配置。新连接首次凭据保存失败时仍可保存无明文配置，凭据仅在当前会话有效。数据库与原生凭据不是跨介质原子事务：数据库失败不会回滚此前成功的凭据写入；移除连接时先删凭据、后提交配置的故障窗口仍需后续收敛。
- 持久化媒体引用需要考虑重启、项目移动和归档恢复，不能仅依赖页面 Blob 或临时签名 URL。
- 项目根 `.thumbnail` 是可重建的画布图片缓存，Windows 设 Hidden，macOS/Linux 使用点目录；复用现有项目/分集共享目录映射。缓存不进入素材索引、孤儿/重复文件扫描或项目导出，分组/外部目录中的同名用户目录保持原语义。前端入口为 `fs/thumbnailCache.ts`，原生 `files/thumbnail_cache.rs` 复核主窗口与真实路径授权，缓存写入不触发资产变更通知。范围与验证见[大画布交互性能优化](./plans/2026-09-09-canvas-interaction-performance.md)。
- `asset://localhost` 与 `http(s)://asset.localhost` 按真实主机名识别为本地素材；网络地址的路径或查询参数中出现相同文字不改变来源。模型/工作流输入先上传本地素材，项目保存对 asset/file 复用授权流式复制，data/blob 继续执行内存大小限制。来源分类不代替网络访问或本地路径权限校验。
- 资源 grant、控制器、窗口句柄和敏感文件正文不进入普通项目持久化；凭据只通过专用服务进入原生凭据存储。
- 导入、归档和递归文件操作必须保留路径校验与私有目录拒绝规则；长传输支持取消与进度。

## 验证与资料

- 资产抽屉回归：[快捷键、搜索、展示模式和读取隔离](../tests/components/assetsPanelDrawer.test.tsx)。已通过浏览器主界面 Tab 开关与输入框焦点检查，并使用隔离素材检查明暗主题、800×600 小窗口和创作资产编辑布局；Tauri 原生文件拖拽仍需桌面实测。
- 节点列表补充回归覆盖全部节点类型、范围与搜索隔离、增删改名、项目切换、增量加载，以及查看按钮的事件、关闭时序和过期保护；该测试文件 18 项回归、应用与测试类型、定向 ESLint 通过。四页签、紧凑列表和搜索已使用隔离页面检查明暗主题；定位与回弹委托既有 Canvas 事件处理，本阶段未在 Tauri 桌面实测动画。
- 节点内容与侧栏连线回归：[卡片资源、端口与指针会话](../tests/components/assetNodeConnection.test.tsx)。覆盖共享预览租约、小图回退、文本/音频、正反向连接、真实 Store 写边与历史快照、重复和非法连接、取消及项目/节点/插件失效。连同抽屉、历史和创作资产回归，共 6 文件 144 项测试、应用/测试类型及定向 ESLint 通过。隔离浏览器页面挂载真实 Canvas 与 Store，已验证明暗主题大卡片、双向拖线、重复拦截和撤销/重做；桌面关闭重开后的持久化尚未现场复测。
- 启动与配置保护回归：[数据库生命周期](../tests/services/indexedDbService.test.ts)、[配置与凭据持久化](../tests/services/configSecretStorage.test.ts)、[配置 Store](../tests/store/config.test.ts)、[项目 Store](../tests/store/projects.test.ts)。覆盖升级中断/占用、异常关闭重连、高版本库拒绝、真实配置读取失败后的禁止覆盖，以及凭据迁移失败保留原记录。自动化使用隔离数据库，不代表故障用户的数据已恢复；真实安装升级、关闭重开和现场恢复仍需独立验收。
- 本次保护修复的 88 项相关回归、应用类型、专项测试类型、定向 ESLint 与 UTF-8 检查通过；未进行真实安装升级或现场数据恢复验收。
- 保存优化回归补充[凭据服务](../tests/services/providerSecretService.test.ts)和[配置持久化](../tests/services/configSecretStorage.test.ts)：覆盖同值免写、读取失败禁止覆盖、已有引用保护、并发保存/迁移/删除顺序、排队快照、事务中止和锁失败后的恢复。相关 7 个测试文件共 117 项回归、应用及测试类型、定向 ESLint 和 UTF-8 检查通过；桌面多实例与真实凭据文件故障仍需运行时验收。

### MCP 资源导入

- `file_import_media_to_canvas`：一次导入 1–20 个已授权本地图片、视频、音频文件，复制进当前项目，按输入顺序返回节点 ID。支持名称、单项坐标和默认网格排列；不修改源文件、不调用生成模型、不自动扩大目录权限。
- `canvas_paste_external`：读取系统图片或纯文本。Windows 桌面端使用仅主窗口可调用的 `read_canvas_clipboard`，支持 PNG / DIB / DIBV5 和 UTF-16 文本；其他平台保留 Web Clipboard 读取并报告权限失败。不跟随 HTML、URL 或文件路径，不使用应用内部节点剪贴板。
- `ui_capture_to_canvas`：把主窗口真实可见界面截图直接持久化为图片节点，始终启用敏感元素过滤；截图字节不进入工具结果。
- 三个入口共用 [资源导入服务](../src/services/canvasResourceImportService.ts) 和既有文件服务、Store 批量写入。整批准备成功后提交一次历史；项目、revision、画布状态变化或取消时不回填。已复制但未提交的媒体可能留在项目素材目录中，不自动删除。
- 原生剪贴板限制输入 32 MiB、文本 100000 UTF-16 单元、图像边长 8192 和解码字节 128 MiB；结果只返回节点 ID、类型、坐标和 revision，不记录路径、剪贴板正文或图像 Base64。
- 验证与回滚见 [资源导入与截图计划](./plans/2026-09-09-mcp资源导入与截图.md)。

- 定向回归：[项目传输](../tests/services/projectTransferService.test.ts)、[项目 Store](../tests/store/projects.test.ts)。涉及落盘时补充关闭重开、缺失媒体、取消和归档往返验收。
- 专项计划：[本地项目媒体引用持久化](./plans/2026-09-02-local-project-media-persistence.md)。
- 历史：[本地媒体持久化第一阶段](./history/2026-09-07-跨模块实施记录归档.md#storage-media)。

返回[文档导航](./文档导航.md)。
