# 插件平台

负责用户插件的安装、更新、启停、信任注册、节点工具执行和自定义 UI。插件接口详细合同统一维护在[插件开发规范](./插件开发规范.md)。

## 主要入口

| 入口 | 职责 |
|---|---|
| [PluginSettings.tsx](../src/components/settings/PluginSettings.tsx) / [store.plugins.ts](../src/store/store.plugins.ts) | 安装管理与原生注册编排 |
| [pluginRuntime.ts](../src/services/plugins/pluginRuntime.ts) / [pluginResourceService.ts](../src/services/plugins/pluginResourceService.ts) | 执行、宿主 effect 和不透明资源授权 |
| [pluginImageService.ts](../src/services/plugins/pluginImageService.ts) | 派生图像的有界本地线稿转换与 PNG 编码 |
| [pluginUiSessionService.ts](../src/services/plugins/pluginUiSessionService.ts) / [pluginUiWindowService.ts](../src/services/plugins/pluginUiWindowService.ts) | 内嵌 UI 会话与独立窗口生命周期 |
| [原生 plugins](../src-tauri/src/plugins/) | 私有快照、活动 revision、执行与 UI 原生边界 |

## 关键边界与当前交互

- 工具和自定义节点的每轮 `input.locale`、自定义界面的 `props.locale` 提供实际生效的宿主语言。内嵌界面实时同步，独立窗口重新聚焦时同步；仅更新语言并通知插件重绘，不重新挂载或覆盖编辑参数。插件自行维护翻译字典，宿主不传递完整配置或字典。

- 运行身份同时绑定 `sourceDigest` 与 `revisionDigest`；原生注册表是执行权威，停用、卸载或更新后旧租约不得继续写回。
- 卸载先停用当前会话并撤销资源授权，原生注册和 IndexedDB 记录均删除成功后才移除列表项；任一步失败都保留停用项供重试，并尝试持久化停用状态，避免重启后造成“已删除插件重新出现”的错觉。注册表 JSON 损坏且无法从备份恢复时，设置页提供显式修复入口；原生二次确认后重建空注册表、终止全部插件调用并清理私有 revision，IndexedDB 安装记录保留但统一停用，供用户卸载或重新安装。权限、路径和普通 I/O 错误不得触发重建。
- 内嵌 UI 使用隔离 iframe；独立 UI 使用专用 WebView。模型、文件 effect 和画布写回由主窗口宿主执行。可信 Python 是当前用户权限的本地代码，不是 OS 沙箱。
- 逐帧拉片默认在主窗口弹窗显示，关闭按钮旁提供独立窗口入口；创建或聚焦成功后关闭原弹窗，失败保留。当前不迁移未保存的镜头编辑或模型选择。
- 逐帧拉片的原视频位于采样方式和分析设置之间；当前预览通过分段读取生成页面 Blob，单视频上限 16 MiB。插件本体由独立仓库维护、用户安装，宿主这里只管理公共能力。
- `image.lineArt` 仅处理当前调用已授权的派生图像，复用已有读取/输出权限及本地媒体操作额度，不调用模型或重复消耗分段读取额度。每个原图 ID 最多缓存一个线稿 PNG，与原图共用 48 MiB 额度，并随资源租约撤销。
- 节点集图像可选择 `original` / `lineart` 表示，省略时使用原图；宿主整批预检后保存实际选定字节，分镜引用同批文件。缺失线稿拒绝输出，保存或最终写回失败回收已写文件。插件需与支持此操作的宿主配套更新，旧宿主不能生成线稿节点。

## 验证与资料

- 定向回归：[执行](../tests/services/pluginRuntime.test.ts)、[UI 会话](../tests/services/pluginUiSessionService.test.ts)、[独立窗口](../tests/services/pluginUiWindowService.test.ts)、[安装 Store](../tests/store/plugins.test.ts)。单元测试与真实 WebView 的 CSP、存储隔离、越权 IPC 及关闭回填验收分别记录。
- 专项计划：[用户插件平台](./plans/2026-08-23-user-plugin-platform.md)、[可信 Python](./plans/2026-08-26-trusted-python-plugins.md)；架构决策：[原生信任注册表](./adr/0010-native-plugin-trust-registry.md)。
- 历史：[平台 MVP](./history/2026-09-07-跨模块实施记录归档.md#plugins-platform)、[独立 WebView](./history/2026-09-07-跨模块实施记录归档.md#plugins-window)、[受限 MCP 验收入口](./history/2026-09-07-跨模块实施记录归档.md#plugins-mcp)、[手动切换与视频预览](./history/2026-09-07-跨模块实施记录归档.md#plugins-frame-review)。历史门禁状态可能已被后续代码取代，继续验收时需检查当前实现。

返回[文档导航](./文档导航.md)。
