# AI Canvas 插件开发规范

> 适用版本：Plugin API v1<br>
> 插件入口：设置 → 插件 → 导入插件文件夹

本文档说明如何为 AI Canvas 编写用户插件。Plugin API v1 允许插件为指定类型的画布节点提供右键工具，读取经过声明的节点内容，并将结构化结果写回当前节点或创建一个新节点。

## 1. 当前能力范围

Plugin API v1 支持：

- 在节点右键菜单中注册一个或多个工具；
- 指定每个工具适用的节点类型；
- 读取 Manifest 中声明的节点顶层字段；
- 返回 JSON 数据更新当前节点；
- 返回 JSON 数据创建派生节点；
- 在独立 QuickJS 沙箱中执行同步 JavaScript。

Plugin API v1 暂不支持：

- 自定义设置面板、节点界面、工具栏按钮或画布面板；
- 网络请求、本地文件读写、系统命令或 Tauri API；
- `import`、`require`、第三方 npm 包或多个 JavaScript 文件；
- Promise、`async` 工具或后台任务；
- 直接处理本地媒体文件、二进制数据或修改受保护的资产字段。

## 2. 插件目录结构

每个插件是一个独立文件夹，根目录必须包含以下两个文件：

```text
my-plugin/
├── manifest.json
└── main.js
```

- 文件名区分大小写；
- `manifest.json` 和 `main.js` 必须位于同一级目录；
- 文件夹中必须且只能有一个 `manifest.json`；
- v1 的入口文件固定为 `main.js`；
- 其他文件不会被插件运行时加载。

## 3. 最小可运行示例

下面的插件会为文本节点增加“输出转大写”右键工具。

### manifest.json

```json
{
  "apiVersion": 1,
  "id": "com.example.uppercase",
  "name": "文本大写工具",
  "version": "1.0.0",
  "author": "Your Name",
  "description": "把文本节点的输出内容转换为大写",
  "category": "content",
  "keywords": ["文本", "格式化"],
  "entry": "main.js",
  "permissions": ["node.read", "node.write"],
  "contributes": {
    "nodeTools": [
      {
        "id": "uppercase-output",
        "title": "输出转大写",
        "description": "把当前节点的 output 转换为大写",
        "placements": ["node-context-menu"],
        "nodeTypes": ["ai-text", "source-text"],
        "inputFields": ["output"],
        "output": {
          "mode": "update-current",
          "fields": ["output"]
        }
      }
    ]
  }
}
```

### main.js

```javascript
definePlugin({
  tools: {
    "uppercase-output": (input) => ({
      data: {
        output: String(input.node.data.output || "").toUpperCase()
      },
      message: "已将节点输出转换为大写"
    })
  }
});
```

安装后，在画布中右键 `ai-text` 或 `source-text` 节点，即可看到“输出转大写”。

## 4. Manifest 顶层字段

| 字段 | 类型 | 必填 | 规则 |
|---|---|---:|---|
| `apiVersion` | number | 是 | v1 固定为 `1` |
| `id` | string | 是 | 插件唯一 ID，建议使用反向域名；仅允许小写字母、数字、点、下划线和短横线，最长 128 字符 |
| `name` | string | 是 | 用户可见名称，最长 80 字符 |
| `version` | string | 是 | 插件版本，最长 32 字符；建议使用语义化版本，如 `1.2.0` |
| `author` | string | 否 | 作者名称，最长 80 字符 |
| `description` | string | 否 | 插件用途说明，最长 240 字符 |
| `category` | string | 是 | 插件分类，见下表 |
| `keywords` | string[] | 否 | 1–12 个关键词，每项最长 128 字符 |
| `entry` | string | 是 | v1 固定为 `main.js` |
| `permissions` | string[] | 是 | 插件权限，见权限章节 |
| `contributes.nodeTools` | object[] | 是 | 插件贡献的节点工具，数量为 1–64 个 |

### category 可选值

| 值 | 含义 |
|---|---|
| `content` | 文本、提示词、结构化内容处理 |
| `media` | 图片、视频、音频相关的元数据处理 |
| `workflow` | 工作流内容或参数处理 |
| `utility` | 不属于以上分类的通用工具 |

分类用于向用户说明插件用途，不会自动授予额外权限。

## 5. 节点工具声明

`contributes.nodeTools` 中的每一项表示一个节点右键工具。

| 字段 | 类型 | 必填 | 规则 |
|---|---|---:|---|
| `id` | string | 是 | 工具在当前插件内的唯一 ID，最长 64 字符；仅允许小写字母、数字、点、下划线和短横线 |
| `title` | string | 是 | 右键菜单中显示的名称，最长 80 字符 |
| `description` | string | 否 | 工具用途说明，最长 240 字符 |
| `placements` | string[] | 是 | v1 只能填写 `node-context-menu` |
| `nodeTypes` | string[] | 是 | 工具适用的节点类型 |
| `inputFields` | string[] | 是 | 允许传给插件的节点数据顶层字段，数量为 1–64 个 |
| `output` | object | 是 | 工具的写入方式和允许输出的字段 |

Manifest 中的工具 ID 必须与 `main.js` 的 `tools` 键完全一致。

### 支持的 nodeTypes

| 节点类型 | 用途 |
|---|---|
| `ai-text` | AI 文本节点 |
| `ai-image` | AI 图片节点 |
| `ai-video` | AI 视频节点 |
| `ai-audio` | AI 音频节点 |
| `ai-animation` | AI 动画节点 |
| `ai-panorama` | AI 全景节点 |
| `ai-markdown` | Markdown 节点 |
| `ai-storyboard` | 宫格分镜节点 |
| `ai-shotlist` | 分镜表节点 |
| `ai-director` | 导演台节点 |
| `source-image` | 图片源节点 |
| `source-video` | 视频源节点 |
| `source-audio` | 音频源节点 |
| `source-text` | 文本源节点 |
| `canvas-note` | 画布笔记 |
| `comment` | 评论节点 |

一个工具可以同时声明多个节点类型：

```json
"nodeTypes": ["ai-text", "source-text", "ai-markdown"]
```

## 6. 输入数据

工具函数接收一个 `input` 对象：

```typescript
interface PluginInput {
  projectId: string;
  node: {
    id: string;
    type: string;
    data: Record<string, JsonValue>;
  };
}
```

其中 `input.node.data` 只包含当前工具在 `inputFields` 中声明、并且可以安全转换为 JSON 的字段。例如：

```json
"inputFields": ["label", "prompt", "output"]
```

对应的插件代码：

```javascript
const label = input.node.data.label;
const prompt = input.node.data.prompt;
const output = input.node.data.output;
```

常用节点字段包括：

| 字段 | 常见类型 | 含义 |
|---|---|---|
| `label` | string | 节点名称 |
| `prompt` | string | 提示词 |
| `output` | string | 文本结果或媒体 URL |
| `status` | string | `idle`、`loading`、`success` 或 `error` |
| `model` | string | 当前模型 ID |
| `provider` | string | 当前供应商 ID |
| `workflowId` | string | 当前工作流 ID |
| `workflowInputs` | object | 工作流输入映射 |
| `imageUrl` | string | 图片展示 URL |
| `videoUrl` | string | 视频展示 URL |
| `audioUrl` | string | 音频展示 URL |
| `thumbnailUrl` | string | 缩略图 URL |
| `style` | string | 画风 ID |
| `aspectRatio` | string | 宽高比 |
| `imageWidth` / `imageHeight` | number | 图片尺寸 |
| `videoWidth` / `videoHeight` | number | 视频尺寸 |
| `note` | object | 画布笔记数据 |

不同节点不一定拥有所有字段。插件必须处理字段不存在或值为空的情况。

以下本地敏感字段禁止出现在 `inputFields` 中：

- `filePath`
- `relativePath`
- `directorCaptureFilePaths`
- `__proto__`
- `constructor`
- `prototype`

## 7. main.js 注册规则

入口文件必须且只能调用一次全局函数 `definePlugin`：

```javascript
definePlugin({
  tools: {
    "tool-id": (input) => {
      return {
        data: {
          output: "插件处理结果"
        },
        message: "可选的完成提示"
      };
    }
  }
});
```

规则如下：

- `tools` 的键是 Manifest 中声明的工具 ID；
- 每个工具值必须是同步函数；
- 工具必须返回可 JSON 序列化的普通对象；
- 返回对象必须包含非空的 `data` 对象；
- `message` 可选，用于执行完成后的短提示，最长保留 240 字符；
- 不要修改 `input`，应根据输入创建并返回新数据；
- 不要返回函数、Symbol、循环引用、`undefined`、Promise 或其他不可 JSON 序列化的值。

Plugin API 的 JSON 值范围为：

```typescript
type JsonValue =
  | null
  | boolean
  | number
  | string
  | JsonValue[]
  | { [key: string]: JsonValue };
```

## 8. 输出规则

### 更新当前节点

使用 `update-current` 将结果合并到当前节点：

```json
"output": {
  "mode": "update-current",
  "fields": ["label", "output"]
}
```

```javascript
definePlugin({
  tools: {
    "format-output": (input) => ({
      data: {
        label: `${input.node.data.label}（已处理）`,
        output: String(input.node.data.output || "").trim()
      }
    })
  }
});
```

### 创建派生节点

使用 `create-node` 在源节点右侧创建新节点：

```json
"output": {
  "mode": "create-node",
  "nodeType": "source-text",
  "fields": ["label", "output"]
}
```

```javascript
definePlugin({
  tools: {
    "create-summary": (input) => ({
      data: {
        label: "内容摘要",
        output: String(input.node.data.output || "").slice(0, 120)
      },
      message: "已创建摘要节点"
    })
  }
});
```

`nodeType` 仅对 `create-node` 有效；省略时沿用源节点类型。未返回 `label` 时，宿主会根据源节点和工具名称生成新节点名称。

插件只能返回 `output.fields` 中预先声明的字段。如果返回未声明字段，整次执行会失败，不会进行部分写入。

以下字段属于宿主身份、本地资产或内部关联信息，禁止写入：

- `type`
- `displayId`
- `filePath`
- `relativePath`
- `assetId`
- `artifactId`
- `role`
- `dramaAssetId`
- `dramaAssetKind`
- `characterLibraryLinks`
- `hiddenByCharacterLibrary`
- `directorInstanceId`
- `directorCaptureFilePaths`
- `__proto__`
- `constructor`
- `prototype`

## 9. 权限声明

v1 支持两项权限：

| 权限 | 含义 |
|---|---|
| `node.read` | 读取 `inputFields` 声明的节点字段 |
| `node.write` | 更新当前节点或创建派生节点 |

节点工具必须声明 `node.write`。只要工具声明了输入字段，也必须声明 `node.read`。当前 v1 的常规配置如下：

```json
"permissions": ["node.read", "node.write"]
```

声明权限不代表插件能绕过字段白名单；运行时仍会分别校验输入和输出字段。

## 10. 安全与资源限制

插件在每次调用时都会创建独立 QuickJS Runtime。宿主只传入裁剪后的 JSON 快照，不向插件暴露 DOM、网络、文件系统或 Tauri 宿主函数。

| 项目 | 限制 |
|---|---:|
| `manifest.json` | 最大 64 KiB |
| `main.js` | 最大 512 KiB |
| 单次输入 JSON | 最大 1 MiB |
| 单次输出 JSON | 最大 1 MiB |
| 沙箱内存 | 最大 64 MiB |
| JavaScript 栈 | 最大 512 KiB |
| 单次执行时间 | 最长 2 秒 |
| 单插件节点工具 | 最多 64 个 |
| 单工具输入字段 | 最多 64 个 |
| 单工具输出字段 | 最多 64 个 |

传入和返回的数据还会进行边界化处理：

- 字符串最长保留 256,000 个字符；
- 数组最多保留 256 项；
- 单个对象最多保留 128 个键；
- JSON 嵌套深度最多 8 层；
- 非有限数字、函数、Symbol 和危险对象键会被拒绝或移除。

画布在插件执行期间发生项目切换、节点变化、插件停用或卸载时，过期结果不会写入画布。

## 11. 安装、更新与卸载

### 安装

1. 打开“设置”；
2. 选择“插件”；
3. 点击“导入插件文件夹”；
4. 选择包含 `manifest.json` 和 `main.js` 的插件目录；
5. 安装成功后，在匹配类型的节点上打开右键菜单。

### 更新

修改文件后重新导入相同 `id` 的插件，会更新 Manifest 和源码，并保留原来的启用或停用状态。

### 停用与卸载

- 停用后，插件工具不会显示在节点菜单中；
- 卸载会移除本地保存的插件 Manifest 和源码；
- 已经写入节点的数据不会随插件卸载而自动撤销，可通过画布历史记录撤销最近操作。

## 12. 常见错误

### 插件工具没有出现在右键菜单

检查：

- 插件是否处于“已启用”状态；
- 当前节点类型是否包含在 `nodeTypes` 中；
- `placements` 是否为 `["node-context-menu"]`；
- Manifest 工具 ID 是否与 `main.js` 中的 `tools` 键一致。

### 安装时报“请求了不允许暴露给插件的本地字段”

`inputFields` 中包含了本地路径或受保护字段。请改为读取安全的展示字段，例如 `output`、`imageUrl` 或 `fileName`。

### 执行时报“返回了未声明字段”

插件返回的 `data` 包含了 `output.fields` 未声明的字段。将需要写入的安全字段加入 Manifest，或者从返回值中删除该字段。

### 执行时报“首版插件工具不支持异步返回值”

工具返回了 Promise，或使用了 `async`。v1 工具必须同步完成并直接返回普通对象。

### 执行超过 2 秒

减少输入规模和循环次数，避免无限循环。超过时间限制的执行会被沙箱中断，结果不会写入画布。

## 13. 发布前检查清单

- [ ] 文件夹根目录包含且只依赖 `manifest.json` 和 `main.js`；
- [ ] `apiVersion` 为 `1`，`entry` 为 `main.js`；
- [ ] 插件 ID 稳定且不会随版本变化；
- [ ] 每个 Manifest 工具 ID 都在 `definePlugin().tools` 中注册；
- [ ] `nodeTypes` 只包含实际支持的节点类型；
- [ ] `inputFields` 只申请工具确实需要的数据；
- [ ] `output.fields` 与工具实际返回的 `data` 字段完全一致；
- [ ] 对缺失、空字符串、空数组等输入进行了处理；
- [ ] 工具同步执行，且正常数据规模下远低于 2 秒；
- [ ] 未使用网络、文件、DOM、Tauri、npm 包或异步 API；
- [ ] 已测试“更新当前节点”和“创建派生节点”是否符合预期；
- [ ] `name`、`description` 和工具标题能让用户理解插件用途与出现位置。

## 14. 兼容性约定

- 宿主根据 `apiVersion` 判断能否加载插件；当前只支持 `1`；
- 插件自身的 `version` 用于展示和发布管理，不决定 API 兼容性；
- 同一插件后续升级时应保持 `id` 不变；
- 已发布工具如需保持用户认知和配置兼容，应尽量保持工具 `id` 不变；
- 未来新增入口位置、权限或异步能力时，将通过新的 API 版本或向后兼容字段开放，插件不得依赖未在本文档中声明的宿主内部对象。

相关架构设计见：[用户插件平台设计](./plans/2026-08-23-user-plugin-platform.md)。
