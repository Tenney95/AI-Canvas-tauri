# AI Canvas 开发指南

> 本文档面向开发者，涵盖本地环境搭建、项目结构、开发约定、调试技巧和常见问题。
> 最后更新: 2026-06-05

## 1. 环境准备

### 1.1 必需工具

| 工具 | 最低版本 | 说明 |
|------|---------|------|
| Node.js | >= 18 | JavaScript 运行时 |
| npm | >= 9 | 包管理器 |
| Rust | >= 1.77 | Tauri 编译需要 |
| Visual Studio Build Tools | 2022 | Windows 上编译 Rust 需要 C++ 工具链 |

### 1.2 Windows 额外配置

安装 [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022)，选择「使用 C++ 的桌面开发」工作负载。

### 1.3 安装依赖

```bash
cd AI-Canvas-tauri
npm install
```

## 2. 启动开发

### 2.1 仅前端开发（不需要 Tauri 运行时）

```bash
npm run dev
```

启动后访问 `http://localhost:1420`。此模式下：
- 文件操作自动降级为浏览器 API（File Input + IndexedDB）
- 原生对话框不可用
- Tauri 插件功能受限

### 2.2 完整 Tauri 桌面应用开发

```bash
npm run tauri dev
```

此命令会：
1. 先启动 Vite 开发服务器（端口 1420）
2. 编译 Rust 后端
3. 打开 Tauri 桌面窗口

### 2.3 生产构建

```bash
npm run build          # 构建前端 (dist/)
npm run tauri build    # 构建桌面安装包
```

## 3. 项目约定

### 3.1 文件命名

| 类型 | 命名规则 | 示例 |
|------|---------|------|
| 组件文件 | PascalCase + `.tsx` | `TextNode.tsx` |
| Hook 文件 | `use` 前缀 + camelCase + `.ts` | `useNodeCreation.ts` |
| 服务文件 | camelCase + `Service.ts` | `fileService.ts` |
| Store 文件 | `store.` 前缀 + 职责 + `.ts` | `store.nodes.ts` |
| 样式文件 | kebab-case + `.css` | `model-selector.css` |
| 类型文件 | `index.ts` | `types/index.ts` |

### 3.2 导入顺序

```tsx
// 1. React / 框架
import { useState, useCallback } from "react";

// 2. 第三方库
import { useReactFlow } from "@xyflow/react";

// 3. 本地组件
import TextNode from "./nodes/TextNode";

// 4. Hooks
import { useAppStore } from "../store/useAppStore";

// 5. 类型
import type { BaseNodeData } from "../types";
```

### 3.3 组件编写规范

- 节点组件用 `React.memo` 包裹
- 接收 `{ data, selected }` props
- 数据变更通过 `useAppStore().updateNodeData(nodeId, data)`
- 禁止在节点组件内直接修改 store 状态

```tsx
// ✅ 正确
const TextNode = React.memo(({ data, selected }: NodeProps) => {
  const updateNodeData = useAppStore((s) => s.updateNodeData);
  // ...
});

// ❌ 错误
const TextNode = ({ data }: NodeProps) => {
  useAppStore.setState({ ... }); // 禁止
};
```

### 3.4 类型定义

所有全局类型定义在 `src/types/index.ts`：
- `NodeType` — 节点类型联合类型
- `BaseNodeData` — 节点数据接口
- `CanvasProject` — 项目元数据
- `AppConfig` / `ApiProviderConfig` — API 配置
- `ModelOption` / `ModelGroup` — 模型选项
- `WorkflowDefinition` — ComfyUI 工作流
- `UserPreset` — 用户预设
- `NodeGroup` — 节点分组

新增类型优先加到这里，禁止在组件文件中重复定义。

### 3.5 样式约定

- 业务样式优先使用 Tailwind class
- 禁止 `!important`、硬编码颜色值、内联 `style.cssText`
- 复用 `tailwind.config.js` 中的 `canvas-*` 颜色 token
- React Flow 样式覆盖统一放 `src/styles/reactflow.css`
- 新增组件独立样式放入 `src/styles/` 目录
- 节点类型 Header 使用语义色：文本=indigo、图像=green、视频=blue、音频=orange

### 3.6 状态管理约定

- 所有状态变更必须通过 Store Actions
- 不可变更新：使用扩展运算符或 `immer`-style 更新
- 新增 slice 在 `src/store/` 下新建 `store.xxx.ts`，在 `useAppStore.ts` 中聚合
- 组件内选择器保持精简，避免不必要的重渲染：

```tsx
// ✅ 精确选择
const nodes = useAppStore((s) => s.nodes);

// ❌ 全量选择（任何状态变更都会重渲染）
const store = useAppStore();
```

## 4. 目录结构

```
AI-Canvas-tauri/
├── index.html                 # Vite 入口 HTML
├── src/
│   ├── main.tsx               # React 应用入口
│   ├── App.tsx                # 根组件装配
│   ├── index.css              # 全局样式入口（导入所有 partial）
│   ├── components/
│   │   ├── Canvas.tsx         # React Flow 画布主组件
│   │   ├── Header.tsx         # 顶部栏
│   │   ├── Sidebar.tsx        # 左侧面板
│   │   ├── Titlebar.tsx       # 自定义窗口标题栏
│   │   ├── NodeMenu.tsx       # 浮动节点菜单
│   │   ├── SettingsPanel.tsx  # 设置弹窗
│   │   ├── AINodeDialog.tsx   # AI 生成弹窗
│   │   ├── WorkflowPanel.tsx  # 工作流管理面板
│   │   ├── AssetsPanel.tsx    # 资产管理面板
│   │   ├── Toast.tsx          # 消息提示
│   │   ├── SplashScreen.tsx   # 开屏动画
│   │   ├── canvas/            # 画布交互子组件
│   │   │   ├── CanvasContextMenu.tsx
│   │   │   ├── NodeContextMenu.tsx
│   │   │   ├── ConnectionMenu.tsx
│   │   │   ├── CanvasToolbar.tsx
│   │   │   └── CanvasEmptyState.tsx
│   │   └── nodes/             # 节点组件
│   │       ├── TextNode.tsx
│   │       ├── ImageNode.tsx
│   │       ├── VideoNode.tsx
│   │       ├── AudioNode.tsx
│   │       ├── GroupNode.tsx
│   │       └── shared/        # 节点共享子组件
│   │           ├── defaultModels.ts
│   │           ├── ModelSelector.tsx
│   │           ├── MentionEditor.tsx
│   │           ├── PromptPanel.tsx
│   │           ├── SlashCommandMenu.tsx
│   │           ├── PresetManager.tsx
│   │           ├── ImageNodeToolbar.tsx
│   │           ├── MattingEditor.tsx
│   │           ├── NodeLabel.tsx
│   │           ├── QualityRatioSelector.tsx
│   │           ├── VideoParamSelector.tsx
│   │           └── ResizeHandle.tsx
│   ├── hooks/                 # 自定义 Hooks
│   │   ├── useNodeCreation.ts
│   │   ├── useKeyboardShortcuts.ts
│   │   ├── useCanvasContextMenu.ts
│   │   ├── useNodeContextMenu.ts
│   │   ├── useConnectionDropMenu.ts
│   │   └── useNodeSnap.ts
│   ├── services/              # 服务层
│   │   ├── aiService.ts
│   │   ├── fileService.ts
│   │   ├── indexedDbService.ts
│   │   ├── uploadService.ts
│   │   ├── testConnection.ts
│   │   └── apimartService.ts
│   ├── store/                 # Zustand 状态管理
│   │   ├── useAppStore.ts     # 聚合入口
│   │   ├── store.nodes.ts
│   │   ├── store.ui.ts
│   │   ├── store.toast.ts
│   │   ├── store.history.ts
│   │   ├── store.config.ts
│   │   ├── store.workflows.ts
│   │   ├── store.presets.ts
│   │   ├── store.groups.ts
│   │   ├── store.clipboard.ts
│   │   ├── store.projects.ts
│   │   └── store.utils.ts
│   ├── types/
│   │   └── index.ts           # 全局类型定义
│   ├── styles/                # 组件样式（CSS partials）
│   │   ├── base.css
│   │   ├── nodes.css
│   │   ├── panels.css
│   │   ├── sidebar.css
│   │   ├── settings.css
│   │   ├── reactflow.css
│   │   ├── model-selector.css
│   │   ├── prompt.css
│   │   ├── preset-manager.css
│   │   ├── quality-ratio.css
│   │   ├── canvas-context.css
│   │   ├── tooltip.css
│   │   └── freeangle.css
│   └── utils/
│       └── popupPosition.ts   # 弹出位置计算工具
├── src-tauri/                 # Tauri Rust 后端
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   └── src/
│       ├── main.rs
│       └── lib.rs
├── doc/                       # 项目文档
├── tailwind.config.js
├── vite.config.ts
├── tsconfig.json
└── package.json
```

## 5. 调试技巧

### 5.1 React DevTools

安装 React DevTools 浏览器扩展，开发模式下在浏览器控制台使用。

### 5.2 React Dev Inspector

开发模式下按 `Ctrl+Shift+C` 可以点击页面元素跳转到源码位置（需要 IDE 支持 `codebuddycn://` 协议）。

### 5.3 Tauri 调试

```bash
# 查看 Tauri 日志
npm run tauri dev

# Rust 后端日志会输出到终端
# 前端 console.log 也会同步到终端
```

### 5.4 IndexedDB 调试

在浏览器 DevTools → Application → IndexedDB → `ai-canvas-db` 查看持久化数据。

### 5.5 Zustand DevTools

可以在 `create()` 中启用 Redux DevTools 集成调试状态变更。

## 6. 新增功能指南

### 6.1 新增节点类型

1. 在 `src/types/index.ts` 的 `NodeType` 中添加新类型
2. 创建 `src/components/nodes/NewTypeNode.tsx`
3. 在 `src/components/Canvas.tsx` 的 `nodeTypes` 映射中注册
4. 在 `src/components/Sidebar.tsx` 的节点列表中添加入口
5. 在 `src/hooks/useNodeCreation.ts` 中添加创建方法
6. 在 `src/services/aiService.ts` 中添加对应的生成适配器（如需要）

### 6.2 新增 AI 提供商

1. 在 `src/services/aiService.ts` 的 `DEFAULT_BASE_URLS` 中添加 base URL
2. 在 `src/components/nodes/shared/defaultModels.ts` 中添加模型配置
3. 在 `src/components/SettingsPanel.tsx` 中添加提供商配置入口
4. 实现对应的请求/响应适配逻辑

### 6.3 新增 Store Slice

1. 创建 `src/store/store.xxx.ts`，导出 `XxxSlice` 接口和 `createXxxSlice` 函数
2. 在 `src/store/useAppStore.ts` 中导入并聚合
3. 在 `AppState` 类型中添加 `& XxxSlice`

## 7. 常见问题

### Q: 前端开发模式下文件操作不可用？
A: 正常现象。Tauri 插件（fs、dialog）仅在 Tauri 桌面环境中可用。浏览器模式下自动降级为 File Input API + IndexedDB。

### Q: 如何清除所有本地数据？
A: 打开浏览器 DevTools → Application → IndexedDB → 删除 `ai-canvas-db` 数据库。

### Q: Tailwind class 不生效？
A: 检查 `tailwind.config.js` 的 `content` 配置是否包含对应文件路径。新增文件后可能需要重启开发服务器。

### Q: Tauri 构建失败？
A: 常见原因：
- Rust 工具链未安装或版本过低
- Visual Studio Build Tools 未安装
- `src-tauri/target/` 缓存损坏：删除后重试
