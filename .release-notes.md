# AI Canvas v0.8.14

## ✨ 版本摘要

AI Canvas v0.8.14 新增分组折叠为文件夹与拖拽入组能力，多选工具栏支持分组 / 取消分组；同步助手独立窗口并修复握手与监听丢失；连接配置支持导入导出与 Base URL 规范化，新增模型协议试跑；工作流可绑定多个 ComfyUI 服务端，项目库支持复制项目与右键快捷操作。

## 🗂️ 分组折叠与拖拽入组

- 分组节点支持折叠为文件夹样式，节省画布空间。
- 支持拖拽节点入组，多选工具栏新增分组 / 取消分组操作。
- 节点右键菜单补充入组入口。

## 💬 助手独立窗口同步

- 新增助手独立窗口同步能力，独立窗口与主窗口状态实时同步。
- 修复独立窗口握手与 StrictMode 下的监听丢失问题。
- 优化独立窗口草稿与投影同步性能。

## ⚙️ 连接配置与协议试跑

- 连接配置支持导入 / 导出，Base URL 自动规范化。
- 优化自定义连接模型区分与上下文窗口处理。
- 新增模型协议试跑，接入前可验证协议可用性，移除 anthropicUrl。

## 🖥️ 工作流与项目库增强

- 工作流支持绑定多个 ComfyUI 服务端，按需切换。
- 项目库支持复制项目与右键快捷操作。

## ⚠️ 升级与兼容性

- 分组折叠、独立窗口同步与协议试跑需要 v0.8.14 或更高版本。
- 视频剪辑器依赖本地编解码能力，实际可用导出编码格式取决于本机编解码器。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。
- 视频剪辑窗口、应用内 ComfyUI、MCP 控制桥、长按圆环和本地文件导出仅在桌面版可用。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
