# AI Canvas v0.9.0

## ✨ 版本摘要

AI Canvas v0.9.0 接通导演台 Blender 原生运行时，支持将场景导出到 Blender 渲染并回传结果；新增全局智能体上传与安全管理，可导入、校验和复用智能体包；统一自定义视频协议与参数映射，支持 APIMart 批量图片任务拆分与多任务恢复；同时加固命令调用权限，并新增 Windows 旧款 CPU 的 Legacy 安装包。

## 🎬 导演台 Blender 原生运行时

- 接通 Blender 原生运行时预览，场景可直接交由 Blender 渲染。
- 自动发现本机 Blender 安装候选，无需手动填写路径。
- 建立场景与结果数据层，冻结 Blender 双运行时协议与前端契约。
- 新增项目目录授权校验，Blender 只能访问授权范围内的资源。

## 🤖 全局智能体上传与安全管理

- 支持上传并导入全局智能体包，内置清单校验与目录登记。
- 新增智能体中心面板，集中查看与管理已导入的智能体。
- 导入过程做安全校验，避免来源不明的智能体被加载。

## 🎥 自定义视频协议统一

- 统一自定义视频协议与参数映射，不同厂商的参数填写方式一致。
- 新增视频请求解析层，提交前统一解析与校验参数。

## 🖼️ APIMart 批量图片任务拆分

- APIMart 批量图片支持任务拆分，大批量请求更稳定。
- 支持多任务恢复，中断后可从断点继续。
- 恢复时同步错误到同组节点，失败状态一目了然。

## 🔒 安全加固

- 收紧 Tauri 应用命令窗口权限，仅允许第一方应用命令被调用。
- 加固插件与导演台命令调用方，校验调用来源。

## 🎨 体验优化

- 助手面板支持拖拽调整宽度，尺寸按会话记忆。
- 插件工具按钮整合进可编辑工具栏注册表，工具栏可自定义。
- 素材参考图节点支持连线菜单。
- 优化助手窄宽布局与任务状态提示。
- 优化浅色主题下节点上传按钮样式。

## 🐛 修复

- 统一节点与连线的渲染层级，连线不再遮挡节点。
- 修复主窗口尺寸恢复与持久化逻辑。
- 修复独立窗口关闭后再次点击无法重新打开的问题。
- 修复 MCP 控制会话无法跨任务访问厂商配置草稿的问题。

## 📦 构建与分发

- 新增 Windows x64 Legacy 安装包，面向不支持 AVX/AVX2 的旧款 CPU。
- 本地 ONNX 能力改为 `local-onnx` feature，可按需裁剪。

## ⚠️ 升级与兼容性

- Blender 原生运行时需要本机已安装 Blender，且需授权项目目录。
- Windows 旧款 CPU（不支持 AVX/AVX2）请使用名称含 `Legacy` 的安装包；该版本不包含本地 ONNX 功能。
- 视频剪辑器依赖本地编解码能力，实际可用导出编码格式取决于本机编解码器。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。
- 视频剪辑窗口、应用内 ComfyUI、MCP 控制桥、长按圆环和本地文件导出仅在桌面版可用。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🪟 Windows 旧款 CPU：使用名称含 `Legacy` 的 `.exe` 或 `.msi`；该版本不包含本地 ONNX 功能。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
