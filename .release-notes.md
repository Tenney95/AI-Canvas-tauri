# AI Canvas v0.8.17

## ✨ 版本摘要

AI Canvas v0.8.17 内置 Sora2U 全部媒体模型并显示余额，新增窗口尺寸预设与固定宽高比锁定、主窗口尺寸记忆；将硬编码光标替换为主题化 CSS 变量（暗/亮两套光标），支持 AI 按当前列生成整张分镜表，并完善日语翻译与画布节点创建基线推进。

## 🎥 Sora2U 厂商接入

- 内置 Sora2U 全部媒体模型，模型菜单显示 Sora2U 厂商。
- 显示 Sora2U 余额并隐藏 2.5 模型，请求自动追加合作参数。

## 🪟 窗口尺寸预设

- 新增窗口尺寸预设与固定宽高比锁定，快速切换常用画布尺寸。
- 记住主窗口尺寸，重新打开恢复上次布局。
- 标记当前使用的尺寸预设。

## 🖱️ 光标主题化

- 将硬编码光标替换为可主题化 CSS 变量，新增暗色与亮色两套光标资源。
- 光标随主题切换，视觉更统一。

## 📐 分镜表 AI 生成

- 支持 AI 按当前列生成整张分镜表，一键补齐整张表格。

## 🧭 圆环菜单扩展

- 圆环菜单加入视频编辑器与动作库入口，动作库可独立打开。

## ⚙️ 其他优化

- 完善日语翻译。
- 优化画布节点创建与连续写工具基线推进。
- 统一重绘按钮为「查看提示词」文案与图标。

## ⚠️ 升级与兼容性

- Sora2U 接入与窗口尺寸预设需要 v0.8.17 或更高版本。
- 视频剪辑器依赖本地编解码能力，实际可用导出编码格式取决于本机编解码器。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。
- 视频剪辑窗口、应用内 ComfyUI、MCP 控制桥、长按圆环和本地文件导出仅在桌面版可用。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
