# AI Canvas v0.8.16

## ✨ 版本摘要

AI Canvas v0.8.16 推出用户节点插件平台：新增插件 Manifest 规范、插件运行时（Rust 侧 rquickjs 沙箱）、GitHub 插件市场与更新追踪、插件设置页与专属插件节点，支持节点工具栏弹窗操作与受限文件能力；同时优化自定义快捷指令，为图片引用添加序号标签，并修复输入法组合、画布边颜色主题适配等问题。

## 🔌 用户节点插件平台

- 新增完整插件体系：插件 Manifest 规范、插件运行时、插件市场与更新追踪。
- 新增专属插件节点（PluginNode），可在画布上运行自定义节点逻辑。
- 新增插件设置页，集中管理已安装插件与权限。
- 支持节点工具栏弹窗操作与受限文件能力，插件能力可扩展。
- Rust 侧新增插件运行时（rquickjs 沙箱），提供受控执行环境。
- 统一插件文件夹上传体验。

## ⚙️ 快捷指令与引用优化

- 优化自定义快捷指令，参数与执行更灵活。
- 为图片引用添加序号标签，@ 提及图片更直观。

## 🐛 修复

- 修复输入法组合及可编辑区域判断问题。
- 修复画布边颜色未适配主题的问题。
- 优化 ComfyUI 设置界面间距与尺寸，修复工作流管理弹窗蒙层可点击关闭。
- 优化节点工具栏悬浮反馈。

## ⚠️ 升级与兼容性

- 插件平台需要 v0.8.16 或更高版本，旧版本无法识别插件节点。
- 视频剪辑器依赖本地编解码能力，实际可用导出编码格式取决于本机编解码器。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。
- 视频剪辑窗口、应用内 ComfyUI、MCP 控制桥、长按圆环和本地文件导出仅在桌面版可用。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
